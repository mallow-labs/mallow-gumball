/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGumballMachineAuthorityPda } from '../../hooked';
import { findAddItemRequestPda, findSellerHistoryPda } from '../pdas';
import { MALLOW_GUMBALL_PROGRAM_ADDRESS } from '../programs';
import {
  getCnftArgsDecoder,
  getCnftArgsEncoder,
  type CnftArgs,
  type CnftArgsArgs,
} from '../types';

export const REQUEST_ADD_CNFT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([189, 159, 171, 46, 145, 188, 38, 32]);

export function getRequestAddCnftDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_ADD_CNFT_DISCRIMINATOR
  );
}

export type RequestAddCnftInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountGumballMachine extends string | AccountMeta<string> = string,
  TAccountSellerHistory extends string | AccountMeta<string> = string,
  TAccountAddItemRequest extends string | AccountMeta<string> = string,
  TAccountAuthorityPda extends string | AccountMeta<string> = string,
  TAccountSeller extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountTreeConfig extends string | AccountMeta<string> = string,
  TAccountMerkleTree extends string | AccountMeta<string> = string,
  TAccountLogWrapper extends string | AccountMeta<string> =
    'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV',
  TAccountCompressionProgram extends string | AccountMeta<string> =
    'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK',
  TAccountBubblegumProgram extends string | AccountMeta<string> =
    'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY',
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballMachine extends string
        ? WritableAccount<TAccountGumballMachine>
        : TAccountGumballMachine,
      TAccountSellerHistory extends string
        ? WritableAccount<TAccountSellerHistory>
        : TAccountSellerHistory,
      TAccountAddItemRequest extends string
        ? WritableAccount<TAccountAddItemRequest>
        : TAccountAddItemRequest,
      TAccountAuthorityPda extends string
        ? WritableAccount<TAccountAuthorityPda>
        : TAccountAuthorityPda,
      TAccountSeller extends string
        ? WritableSignerAccount<TAccountSeller> &
            AccountSignerMeta<TAccountSeller>
        : TAccountSeller,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountTreeConfig extends string
        ? ReadonlyAccount<TAccountTreeConfig>
        : TAccountTreeConfig,
      TAccountMerkleTree extends string
        ? WritableAccount<TAccountMerkleTree>
        : TAccountMerkleTree,
      TAccountLogWrapper extends string
        ? ReadonlyAccount<TAccountLogWrapper>
        : TAccountLogWrapper,
      TAccountCompressionProgram extends string
        ? ReadonlyAccount<TAccountCompressionProgram>
        : TAccountCompressionProgram,
      TAccountBubblegumProgram extends string
        ? ReadonlyAccount<TAccountBubblegumProgram>
        : TAccountBubblegumProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RequestAddCnftInstructionData = {
  discriminator: ReadonlyUint8Array;
  args: CnftArgs;
};

export type RequestAddCnftInstructionDataArgs = { args: CnftArgsArgs };

export function getRequestAddCnftInstructionDataEncoder(): Encoder<RequestAddCnftInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['args', getCnftArgsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_ADD_CNFT_DISCRIMINATOR })
  );
}

export function getRequestAddCnftInstructionDataDecoder(): Decoder<RequestAddCnftInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['args', getCnftArgsDecoder()],
  ]);
}

export function getRequestAddCnftInstructionDataCodec(): Codec<
  RequestAddCnftInstructionDataArgs,
  RequestAddCnftInstructionData
> {
  return combineCodec(
    getRequestAddCnftInstructionDataEncoder(),
    getRequestAddCnftInstructionDataDecoder()
  );
}

export type RequestAddCnftAsyncInput<
  TAccountGumballMachine extends string = string,
  TAccountSellerHistory extends string = string,
  TAccountAddItemRequest extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountSeller extends string = string,
  TAccountAsset extends string = string,
  TAccountTreeConfig extends string = string,
  TAccountMerkleTree extends string = string,
  TAccountLogWrapper extends string = string,
  TAccountCompressionProgram extends string = string,
  TAccountBubblegumProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Gumball Machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Seller history account. */
  sellerHistory?: Address<TAccountSellerHistory>;
  /** Add item request account, keyed by the asset id. */
  addItemRequest?: Address<TAccountAddItemRequest>;
  authorityPda?: Address<TAccountAuthorityPda>;
  /** Seller of the cNFT (current leaf owner). */
  seller: TransactionSigner<TAccountSeller>;
  /**
   * The cNFT asset id (a Bubblegum PDA; not a real account). Used only as the
   * request PDA seed and bound to (merkle_tree, nonce) in the handler.
   */
  asset: Address<TAccountAsset>;
  treeConfig: Address<TAccountTreeConfig>;
  merkleTree: Address<TAccountMerkleTree>;
  logWrapper?: Address<TAccountLogWrapper>;
  compressionProgram?: Address<TAccountCompressionProgram>;
  bubblegumProgram?: Address<TAccountBubblegumProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  args: RequestAddCnftInstructionDataArgs['args'];
};

export async function getRequestAddCnftInstructionAsync<
  TAccountGumballMachine extends string,
  TAccountSellerHistory extends string,
  TAccountAddItemRequest extends string,
  TAccountAuthorityPda extends string,
  TAccountSeller extends string,
  TAccountAsset extends string,
  TAccountTreeConfig extends string,
  TAccountMerkleTree extends string,
  TAccountLogWrapper extends string,
  TAccountCompressionProgram extends string,
  TAccountBubblegumProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: RequestAddCnftAsyncInput<
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountSeller,
    TAccountAsset,
    TAccountTreeConfig,
    TAccountMerkleTree,
    TAccountLogWrapper,
    TAccountCompressionProgram,
    TAccountBubblegumProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RequestAddCnftInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountSeller,
    TAccountAsset,
    TAccountTreeConfig,
    TAccountMerkleTree,
    TAccountLogWrapper,
    TAccountCompressionProgram,
    TAccountBubblegumProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    sellerHistory: { value: input.sellerHistory ?? null, isWritable: true },
    addItemRequest: { value: input.addItemRequest ?? null, isWritable: true },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    seller: { value: input.seller ?? null, isWritable: true },
    asset: { value: input.asset ?? null, isWritable: false },
    treeConfig: { value: input.treeConfig ?? null, isWritable: false },
    merkleTree: { value: input.merkleTree ?? null, isWritable: true },
    logWrapper: { value: input.logWrapper ?? null, isWritable: false },
    compressionProgram: {
      value: input.compressionProgram ?? null,
      isWritable: false,
    },
    bubblegumProgram: {
      value: input.bubblegumProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.sellerHistory.value) {
    accounts.sellerHistory.value = await findSellerHistoryPda({
      seller: getAddressFromResolvedInstructionAccount(
        'seller',
        accounts.seller.value
      ),
      gumballMachine: getAddressFromResolvedInstructionAccount(
        'gumballMachine',
        accounts.gumballMachine.value
      ),
    });
  }
  if (!accounts.addItemRequest.value) {
    accounts.addItemRequest.value = await findAddItemRequestPda({
      asset: getAddressFromResolvedInstructionAccount(
        'asset',
        accounts.asset.value
      ),
    });
  }
  if (!accounts.authorityPda.value) {
    accounts.authorityPda.value = await findGumballMachineAuthorityPda({
      gumballMachine: getAddressFromResolvedInstructionAccount(
        'gumballMachine',
        accounts.gumballMachine.value
      ),
    });
  }
  if (!accounts.logWrapper.value) {
    accounts.logWrapper.value =
      'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV' as Address<'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV'>;
  }
  if (!accounts.compressionProgram.value) {
    accounts.compressionProgram.value =
      'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK' as Address<'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'>;
  }
  if (!accounts.bubblegumProgram.value) {
    accounts.bubblegumProgram.value =
      'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY' as Address<'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('sellerHistory', accounts.sellerHistory),
      getAccountMeta('addItemRequest', accounts.addItemRequest),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('asset', accounts.asset),
      getAccountMeta('treeConfig', accounts.treeConfig),
      getAccountMeta('merkleTree', accounts.merkleTree),
      getAccountMeta('logWrapper', accounts.logWrapper),
      getAccountMeta('compressionProgram', accounts.compressionProgram),
      getAccountMeta('bubblegumProgram', accounts.bubblegumProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRequestAddCnftInstructionDataEncoder().encode(
      args as RequestAddCnftInstructionDataArgs
    ),
    programAddress,
  } as RequestAddCnftInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountSeller,
    TAccountAsset,
    TAccountTreeConfig,
    TAccountMerkleTree,
    TAccountLogWrapper,
    TAccountCompressionProgram,
    TAccountBubblegumProgram,
    TAccountSystemProgram
  >);
}

export type RequestAddCnftInput<
  TAccountGumballMachine extends string = string,
  TAccountSellerHistory extends string = string,
  TAccountAddItemRequest extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountSeller extends string = string,
  TAccountAsset extends string = string,
  TAccountTreeConfig extends string = string,
  TAccountMerkleTree extends string = string,
  TAccountLogWrapper extends string = string,
  TAccountCompressionProgram extends string = string,
  TAccountBubblegumProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Gumball Machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Seller history account. */
  sellerHistory: Address<TAccountSellerHistory>;
  /** Add item request account, keyed by the asset id. */
  addItemRequest: Address<TAccountAddItemRequest>;
  authorityPda: Address<TAccountAuthorityPda>;
  /** Seller of the cNFT (current leaf owner). */
  seller: TransactionSigner<TAccountSeller>;
  /**
   * The cNFT asset id (a Bubblegum PDA; not a real account). Used only as the
   * request PDA seed and bound to (merkle_tree, nonce) in the handler.
   */
  asset: Address<TAccountAsset>;
  treeConfig: Address<TAccountTreeConfig>;
  merkleTree: Address<TAccountMerkleTree>;
  logWrapper?: Address<TAccountLogWrapper>;
  compressionProgram?: Address<TAccountCompressionProgram>;
  bubblegumProgram?: Address<TAccountBubblegumProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  args: RequestAddCnftInstructionDataArgs['args'];
};

export function getRequestAddCnftInstruction<
  TAccountGumballMachine extends string,
  TAccountSellerHistory extends string,
  TAccountAddItemRequest extends string,
  TAccountAuthorityPda extends string,
  TAccountSeller extends string,
  TAccountAsset extends string,
  TAccountTreeConfig extends string,
  TAccountMerkleTree extends string,
  TAccountLogWrapper extends string,
  TAccountCompressionProgram extends string,
  TAccountBubblegumProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: RequestAddCnftInput<
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountSeller,
    TAccountAsset,
    TAccountTreeConfig,
    TAccountMerkleTree,
    TAccountLogWrapper,
    TAccountCompressionProgram,
    TAccountBubblegumProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RequestAddCnftInstruction<
  TProgramAddress,
  TAccountGumballMachine,
  TAccountSellerHistory,
  TAccountAddItemRequest,
  TAccountAuthorityPda,
  TAccountSeller,
  TAccountAsset,
  TAccountTreeConfig,
  TAccountMerkleTree,
  TAccountLogWrapper,
  TAccountCompressionProgram,
  TAccountBubblegumProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    sellerHistory: { value: input.sellerHistory ?? null, isWritable: true },
    addItemRequest: { value: input.addItemRequest ?? null, isWritable: true },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    seller: { value: input.seller ?? null, isWritable: true },
    asset: { value: input.asset ?? null, isWritable: false },
    treeConfig: { value: input.treeConfig ?? null, isWritable: false },
    merkleTree: { value: input.merkleTree ?? null, isWritable: true },
    logWrapper: { value: input.logWrapper ?? null, isWritable: false },
    compressionProgram: {
      value: input.compressionProgram ?? null,
      isWritable: false,
    },
    bubblegumProgram: {
      value: input.bubblegumProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.logWrapper.value) {
    accounts.logWrapper.value =
      'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV' as Address<'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV'>;
  }
  if (!accounts.compressionProgram.value) {
    accounts.compressionProgram.value =
      'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK' as Address<'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'>;
  }
  if (!accounts.bubblegumProgram.value) {
    accounts.bubblegumProgram.value =
      'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY' as Address<'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('sellerHistory', accounts.sellerHistory),
      getAccountMeta('addItemRequest', accounts.addItemRequest),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('asset', accounts.asset),
      getAccountMeta('treeConfig', accounts.treeConfig),
      getAccountMeta('merkleTree', accounts.merkleTree),
      getAccountMeta('logWrapper', accounts.logWrapper),
      getAccountMeta('compressionProgram', accounts.compressionProgram),
      getAccountMeta('bubblegumProgram', accounts.bubblegumProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRequestAddCnftInstructionDataEncoder().encode(
      args as RequestAddCnftInstructionDataArgs
    ),
    programAddress,
  } as RequestAddCnftInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountSeller,
    TAccountAsset,
    TAccountTreeConfig,
    TAccountMerkleTree,
    TAccountLogWrapper,
    TAccountCompressionProgram,
    TAccountBubblegumProgram,
    TAccountSystemProgram
  >);
}

export type ParsedRequestAddCnftInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Gumball Machine account. */
    gumballMachine: TAccountMetas[0];
    /** Seller history account. */
    sellerHistory: TAccountMetas[1];
    /** Add item request account, keyed by the asset id. */
    addItemRequest: TAccountMetas[2];
    authorityPda: TAccountMetas[3];
    /** Seller of the cNFT (current leaf owner). */
    seller: TAccountMetas[4];
    /**
     * The cNFT asset id (a Bubblegum PDA; not a real account). Used only as the
     * request PDA seed and bound to (merkle_tree, nonce) in the handler.
     */
    asset: TAccountMetas[5];
    treeConfig: TAccountMetas[6];
    merkleTree: TAccountMetas[7];
    logWrapper: TAccountMetas[8];
    compressionProgram: TAccountMetas[9];
    bubblegumProgram: TAccountMetas[10];
    systemProgram: TAccountMetas[11];
  };
  data: RequestAddCnftInstructionData;
};

export function parseRequestAddCnftInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRequestAddCnftInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 12) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 12,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gumballMachine: getNextAccount(),
      sellerHistory: getNextAccount(),
      addItemRequest: getNextAccount(),
      authorityPda: getNextAccount(),
      seller: getNextAccount(),
      asset: getNextAccount(),
      treeConfig: getNextAccount(),
      merkleTree: getNextAccount(),
      logWrapper: getNextAccount(),
      compressionProgram: getNextAccount(),
      bubblegumProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRequestAddCnftInstructionDataDecoder().decode(instruction.data),
  };
}
