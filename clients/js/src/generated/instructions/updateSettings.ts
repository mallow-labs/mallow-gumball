/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  none,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { MALLOW_GUMBALL_PROGRAM_ADDRESS } from '../programs';
import {
  getBuyBackConfigDecoder,
  getBuyBackConfigEncoder,
  getGumballSettingsDecoder,
  getGumballSettingsEncoder,
  type BuyBackConfig,
  type BuyBackConfigArgs,
  type GumballSettings,
  type GumballSettingsArgs,
} from '../types';

export const UPDATE_SETTINGS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [81, 166, 51, 213, 158, 84, 157, 108]
);

export function getUpdateSettingsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_SETTINGS_DISCRIMINATOR
  );
}

export type UpdateSettingsInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountGumballMachine extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballMachine extends string
        ? WritableAccount<TAccountGumballMachine>
        : TAccountGumballMachine,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateSettingsInstructionData = {
  discriminator: ReadonlyUint8Array;
  settings: GumballSettings;
  buyBackConfig: Option<BuyBackConfig>;
};

export type UpdateSettingsInstructionDataArgs = {
  settings: GumballSettingsArgs;
  buyBackConfig?: OptionOrNullable<BuyBackConfigArgs>;
};

export function getUpdateSettingsInstructionDataEncoder(): Encoder<UpdateSettingsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['settings', getGumballSettingsEncoder()],
      ['buyBackConfig', getOptionEncoder(getBuyBackConfigEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_SETTINGS_DISCRIMINATOR,
      buyBackConfig: value.buyBackConfig ?? none(),
    })
  );
}

export function getUpdateSettingsInstructionDataDecoder(): Decoder<UpdateSettingsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['settings', getGumballSettingsDecoder()],
    ['buyBackConfig', getOptionDecoder(getBuyBackConfigDecoder())],
  ]);
}

export function getUpdateSettingsInstructionDataCodec(): Codec<
  UpdateSettingsInstructionDataArgs,
  UpdateSettingsInstructionData
> {
  return combineCodec(
    getUpdateSettingsInstructionDataEncoder(),
    getUpdateSettingsInstructionDataDecoder()
  );
}

export type UpdateSettingsInput<
  TAccountGumballMachine extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** Gumball machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Gumball Machine authority. This is the address that controls the upate of the gumball machine. */
  authority: TransactionSigner<TAccountAuthority>;
  settings: UpdateSettingsInstructionDataArgs['settings'];
  buyBackConfig?: UpdateSettingsInstructionDataArgs['buyBackConfig'];
};

export function getUpdateSettingsInstruction<
  TAccountGumballMachine extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: UpdateSettingsInput<TAccountGumballMachine, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateSettingsInstruction<
  TProgramAddress,
  TAccountGumballMachine,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('authority', accounts.authority),
    ],
    data: getUpdateSettingsInstructionDataEncoder().encode(
      args as UpdateSettingsInstructionDataArgs
    ),
    programAddress,
  } as UpdateSettingsInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAuthority
  >);
}

export type ParsedUpdateSettingsInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Gumball machine account. */
    gumballMachine: TAccountMetas[0];
    /** Gumball Machine authority. This is the address that controls the upate of the gumball machine. */
    authority: TAccountMetas[1];
  };
  data: UpdateSettingsInstructionData;
};

export function parseUpdateSettingsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateSettingsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { gumballMachine: getNextAccount(), authority: getNextAccount() },
    data: getUpdateSettingsInstructionDataDecoder().decode(instruction.data),
  };
}
