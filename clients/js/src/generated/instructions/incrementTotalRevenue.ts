/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { MALLOW_GUMBALL_PROGRAM_ADDRESS } from '../programs';

export const INCREMENT_TOTAL_REVENUE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([197, 168, 14, 157, 91, 203, 104, 102]);

export function getIncrementTotalRevenueDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INCREMENT_TOTAL_REVENUE_DISCRIMINATOR
  );
}

export type IncrementTotalRevenueInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountGumballMachine extends string | AccountMeta<string> = string,
  TAccountMintAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballMachine extends string
        ? WritableAccount<TAccountGumballMachine>
        : TAccountGumballMachine,
      TAccountMintAuthority extends string
        ? ReadonlySignerAccount<TAccountMintAuthority> &
            AccountSignerMeta<TAccountMintAuthority>
        : TAccountMintAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type IncrementTotalRevenueInstructionData = {
  discriminator: ReadonlyUint8Array;
  revenue: bigint;
};

export type IncrementTotalRevenueInstructionDataArgs = {
  revenue: number | bigint;
};

export function getIncrementTotalRevenueInstructionDataEncoder(): FixedSizeEncoder<IncrementTotalRevenueInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['revenue', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INCREMENT_TOTAL_REVENUE_DISCRIMINATOR,
    })
  );
}

export function getIncrementTotalRevenueInstructionDataDecoder(): FixedSizeDecoder<IncrementTotalRevenueInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['revenue', getU64Decoder()],
  ]);
}

export function getIncrementTotalRevenueInstructionDataCodec(): FixedSizeCodec<
  IncrementTotalRevenueInstructionDataArgs,
  IncrementTotalRevenueInstructionData
> {
  return combineCodec(
    getIncrementTotalRevenueInstructionDataEncoder(),
    getIncrementTotalRevenueInstructionDataDecoder()
  );
}

export type IncrementTotalRevenueInput<
  TAccountGumballMachine extends string = string,
  TAccountMintAuthority extends string = string,
> = {
  /** Gumball machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Gumball machine mint authority (mint only allowed for the mint_authority). */
  mintAuthority: TransactionSigner<TAccountMintAuthority>;
  revenue: IncrementTotalRevenueInstructionDataArgs['revenue'];
};

export function getIncrementTotalRevenueInstruction<
  TAccountGumballMachine extends string,
  TAccountMintAuthority extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: IncrementTotalRevenueInput<
    TAccountGumballMachine,
    TAccountMintAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): IncrementTotalRevenueInstruction<
  TProgramAddress,
  TAccountGumballMachine,
  TAccountMintAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    mintAuthority: { value: input.mintAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('mintAuthority', accounts.mintAuthority),
    ],
    data: getIncrementTotalRevenueInstructionDataEncoder().encode(
      args as IncrementTotalRevenueInstructionDataArgs
    ),
    programAddress,
  } as IncrementTotalRevenueInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountMintAuthority
  >);
}

export type ParsedIncrementTotalRevenueInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Gumball machine account. */
    gumballMachine: TAccountMetas[0];
    /** Gumball machine mint authority (mint only allowed for the mint_authority). */
    mintAuthority: TAccountMetas[1];
  };
  data: IncrementTotalRevenueInstructionData;
};

export function parseIncrementTotalRevenueInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedIncrementTotalRevenueInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gumballMachine: getNextAccount(),
      mintAuthority: getNextAccount(),
    },
    data: getIncrementTotalRevenueInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
