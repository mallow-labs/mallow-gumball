/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findGumballMachineAuthorityPda } from '../../hooked';
import { findSellerHistoryPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import { CnftArgs, CnftArgsArgs, getCnftArgsSerializer } from '../types';

// Accounts.
export type RemoveCnftInstructionAccounts = {
  /** Gumball Machine account. */
  gumballMachine: PublicKey | Pda;
  /** Seller history account. */
  sellerHistory?: PublicKey | Pda;
  authorityPda?: PublicKey | Pda;
  /** Authority allowed to remove (gumball machine authority or item seller). */
  authority?: Signer;
  seller?: PublicKey | Pda;
  treeConfig: PublicKey | Pda;
  /** derived asset id passed into `remove_multiple_items_span`. */
  merkleTree: PublicKey | Pda;
  logWrapper?: PublicKey | Pda;
  compressionProgram?: PublicKey | Pda;
  bubblegumProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RemoveCnftInstructionData = {
  discriminator: Array<number>;
  index: number;
  args: CnftArgs;
};

export type RemoveCnftInstructionDataArgs = {
  index: number;
  args: CnftArgsArgs;
};

export function getRemoveCnftInstructionDataSerializer(): Serializer<
  RemoveCnftInstructionDataArgs,
  RemoveCnftInstructionData
> {
  return mapSerializer<
    RemoveCnftInstructionDataArgs,
    any,
    RemoveCnftInstructionData
  >(
    struct<RemoveCnftInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['index', u32()],
        ['args', getCnftArgsSerializer()],
      ],
      { description: 'RemoveCnftInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [24, 204, 40, 184, 195, 73, 136, 210],
    })
  ) as Serializer<RemoveCnftInstructionDataArgs, RemoveCnftInstructionData>;
}

// Args.
export type RemoveCnftInstructionArgs = RemoveCnftInstructionDataArgs;

// Instruction.
export function removeCnft(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: RemoveCnftInstructionAccounts & RemoveCnftInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mallowGumball',
    'MGUMqztv7MHgoHBYWbvMyL3E3NJ4UHfTwgLJUQAbKGa'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    gumballMachine: {
      index: 0,
      isWritable: true,
      value: input.gumballMachine ?? null,
    },
    sellerHistory: {
      index: 1,
      isWritable: true,
      value: input.sellerHistory ?? null,
    },
    authorityPda: {
      index: 2,
      isWritable: true,
      value: input.authorityPda ?? null,
    },
    authority: { index: 3, isWritable: false, value: input.authority ?? null },
    seller: { index: 4, isWritable: true, value: input.seller ?? null },
    treeConfig: {
      index: 5,
      isWritable: false,
      value: input.treeConfig ?? null,
    },
    merkleTree: { index: 6, isWritable: true, value: input.merkleTree ?? null },
    logWrapper: {
      index: 7,
      isWritable: false,
      value: input.logWrapper ?? null,
    },
    compressionProgram: {
      index: 8,
      isWritable: false,
      value: input.compressionProgram ?? null,
    },
    bubblegumProgram: {
      index: 9,
      isWritable: false,
      value: input.bubblegumProgram ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false,
      value: input.systemProgram ?? null,
    },
  };

  // Arguments.
  const resolvedArgs: RemoveCnftInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.seller.value) {
    resolvedAccounts.seller.value = context.identity.publicKey;
  }
  if (!resolvedAccounts.sellerHistory.value) {
    resolvedAccounts.sellerHistory.value = findSellerHistoryPda(context, {
      gumballMachine: expectPublicKey(resolvedAccounts.gumballMachine.value),
      seller: expectPublicKey(resolvedAccounts.seller.value),
    });
  }
  if (!resolvedAccounts.authorityPda.value) {
    resolvedAccounts.authorityPda.value = findGumballMachineAuthorityPda(
      context,
      { gumballMachine: expectPublicKey(resolvedAccounts.gumballMachine.value) }
    );
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.logWrapper.value) {
    resolvedAccounts.logWrapper.value = context.programs.getPublicKey(
      'splNoop',
      'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV'
    );
    resolvedAccounts.logWrapper.isWritable = false;
  }
  if (!resolvedAccounts.compressionProgram.value) {
    resolvedAccounts.compressionProgram.value = context.programs.getPublicKey(
      'splAccountCompression',
      'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'
    );
    resolvedAccounts.compressionProgram.isWritable = false;
  }
  if (!resolvedAccounts.bubblegumProgram.value) {
    resolvedAccounts.bubblegumProgram.value = context.programs.getPublicKey(
      'mplBubblegum',
      'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY'
    );
    resolvedAccounts.bubblegumProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveCnftInstructionDataSerializer().serialize(
    resolvedArgs as RemoveCnftInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
