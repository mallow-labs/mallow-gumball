/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  none,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGumballMachineAuthorityPda } from '../../hooked';
import { MALLOW_GUMBALL_PROGRAM_ADDRESS } from '../programs';
import {
  getBuyBackConfigDecoder,
  getBuyBackConfigEncoder,
  getFeeConfigDecoder,
  getFeeConfigEncoder,
  getGumballSettingsDecoder,
  getGumballSettingsEncoder,
  type BuyBackConfig,
  type BuyBackConfigArgs,
  type FeeConfig,
  type FeeConfigArgs,
  type GumballSettings,
  type GumballSettingsArgs,
} from '../types';

export const INITIALIZE_GUMBALL_MACHINE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237]);

export function getInitializeGumballMachineDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_GUMBALL_MACHINE_DISCRIMINATOR
  );
}

export type InitializeGumballMachineInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountGumballMachine extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAuthorityPda extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballMachine extends string
        ? WritableAccount<TAccountGumballMachine>
        : TAccountGumballMachine,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountAuthorityPda extends string
        ? WritableAccount<TAccountAuthorityPda>
        : TAccountAuthorityPda,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeGumballMachineInstructionData = {
  discriminator: ReadonlyUint8Array;
  settings: GumballSettings;
  feeConfig: Option<FeeConfig>;
  disablePrimarySplit: boolean;
  buyBackConfig: Option<BuyBackConfig>;
  disableRoyalties: boolean;
};

export type InitializeGumballMachineInstructionDataArgs = {
  settings: GumballSettingsArgs;
  feeConfig?: OptionOrNullable<FeeConfigArgs>;
  disablePrimarySplit?: boolean;
  buyBackConfig?: OptionOrNullable<BuyBackConfigArgs>;
  disableRoyalties?: boolean;
};

export function getInitializeGumballMachineInstructionDataEncoder(): Encoder<InitializeGumballMachineInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['settings', getGumballSettingsEncoder()],
      ['feeConfig', getOptionEncoder(getFeeConfigEncoder())],
      ['disablePrimarySplit', getBooleanEncoder()],
      ['buyBackConfig', getOptionEncoder(getBuyBackConfigEncoder())],
      ['disableRoyalties', getBooleanEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_GUMBALL_MACHINE_DISCRIMINATOR,
      feeConfig: value.feeConfig ?? none(),
      disablePrimarySplit: value.disablePrimarySplit ?? false,
      buyBackConfig: value.buyBackConfig ?? none(),
      disableRoyalties: value.disableRoyalties ?? false,
    })
  );
}

export function getInitializeGumballMachineInstructionDataDecoder(): Decoder<InitializeGumballMachineInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['settings', getGumballSettingsDecoder()],
    ['feeConfig', getOptionDecoder(getFeeConfigDecoder())],
    ['disablePrimarySplit', getBooleanDecoder()],
    ['buyBackConfig', getOptionDecoder(getBuyBackConfigDecoder())],
    ['disableRoyalties', getBooleanDecoder()],
  ]);
}

export function getInitializeGumballMachineInstructionDataCodec(): Codec<
  InitializeGumballMachineInstructionDataArgs,
  InitializeGumballMachineInstructionData
> {
  return combineCodec(
    getInitializeGumballMachineInstructionDataEncoder(),
    getInitializeGumballMachineInstructionDataDecoder()
  );
}

export type InitializeGumballMachineAsyncInput<
  TAccountGumballMachine extends string = string,
  TAccountAuthority extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Gumball Machine account. The account space must be allocated to allow accounts larger
   * than 10kb.
   *
   */
  gumballMachine: Address<TAccountGumballMachine>;
  /**
   * Gumball Machine authority. This is the address that controls the upate of the gumball machine.
   *
   */
  authority: Address<TAccountAuthority>;
  authorityPda?: Address<TAccountAuthorityPda>;
  /** Payer of the transaction. */
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  settings: InitializeGumballMachineInstructionDataArgs['settings'];
  feeConfig?: InitializeGumballMachineInstructionDataArgs['feeConfig'];
  disablePrimarySplit?: InitializeGumballMachineInstructionDataArgs['disablePrimarySplit'];
  buyBackConfig?: InitializeGumballMachineInstructionDataArgs['buyBackConfig'];
  disableRoyalties?: InitializeGumballMachineInstructionDataArgs['disableRoyalties'];
};

export async function getInitializeGumballMachineInstructionAsync<
  TAccountGumballMachine extends string,
  TAccountAuthority extends string,
  TAccountAuthorityPda extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: InitializeGumballMachineAsyncInput<
    TAccountGumballMachine,
    TAccountAuthority,
    TAccountAuthorityPda,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeGumballMachineInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAuthority,
    TAccountAuthorityPda,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.authorityPda.value) {
    accounts.authorityPda.value = await findGumballMachineAuthorityPda({
      gumballMachine: getAddressFromResolvedInstructionAccount(
        'gumballMachine',
        accounts.gumballMachine.value
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getInitializeGumballMachineInstructionDataEncoder().encode(
      args as InitializeGumballMachineInstructionDataArgs
    ),
    programAddress,
  } as InitializeGumballMachineInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAuthority,
    TAccountAuthorityPda,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type InitializeGumballMachineInput<
  TAccountGumballMachine extends string = string,
  TAccountAuthority extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Gumball Machine account. The account space must be allocated to allow accounts larger
   * than 10kb.
   *
   */
  gumballMachine: Address<TAccountGumballMachine>;
  /**
   * Gumball Machine authority. This is the address that controls the upate of the gumball machine.
   *
   */
  authority: Address<TAccountAuthority>;
  authorityPda: Address<TAccountAuthorityPda>;
  /** Payer of the transaction. */
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  settings: InitializeGumballMachineInstructionDataArgs['settings'];
  feeConfig?: InitializeGumballMachineInstructionDataArgs['feeConfig'];
  disablePrimarySplit?: InitializeGumballMachineInstructionDataArgs['disablePrimarySplit'];
  buyBackConfig?: InitializeGumballMachineInstructionDataArgs['buyBackConfig'];
  disableRoyalties?: InitializeGumballMachineInstructionDataArgs['disableRoyalties'];
};

export function getInitializeGumballMachineInstruction<
  TAccountGumballMachine extends string,
  TAccountAuthority extends string,
  TAccountAuthorityPda extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: InitializeGumballMachineInput<
    TAccountGumballMachine,
    TAccountAuthority,
    TAccountAuthorityPda,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeGumballMachineInstruction<
  TProgramAddress,
  TAccountGumballMachine,
  TAccountAuthority,
  TAccountAuthorityPda,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getInitializeGumballMachineInstructionDataEncoder().encode(
      args as InitializeGumballMachineInstructionDataArgs
    ),
    programAddress,
  } as InitializeGumballMachineInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAuthority,
    TAccountAuthorityPda,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeGumballMachineInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Gumball Machine account. The account space must be allocated to allow accounts larger
     * than 10kb.
     *
     */
    gumballMachine: TAccountMetas[0];
    /**
     * Gumball Machine authority. This is the address that controls the upate of the gumball machine.
     *
     */
    authority: TAccountMetas[1];
    authorityPda: TAccountMetas[2];
    /** Payer of the transaction. */
    payer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeGumballMachineInstructionData;
};

export function parseInitializeGumballMachineInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeGumballMachineInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gumballMachine: getNextAccount(),
      authority: getNextAccount(),
      authorityPda: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeGumballMachineInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
