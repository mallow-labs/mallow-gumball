/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { Context, Option, OptionOrNullable, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi';
import { Serializer, bytes, mapSerializer, option, publicKey as publicKeySerializer, struct } from '@metaplex-foundation/umi/serializers';
import { findGlobalConfigPda } from '../accounts';
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared';

// Accounts.
export type UpdateGlobalConfigInstructionAccounts = {
      /** The current config authority — must match global_config.config_authority. */
    authority?: Signer;
    globalConfig?: PublicKey | Pda;
};

  // Data.
  export type UpdateGlobalConfigInstructionData = { discriminator: Uint8Array; newConfigAuthority: Option<PublicKey>; newAccountFeeAuthority: Option<PublicKey>;  };

export type UpdateGlobalConfigInstructionDataArgs = { newConfigAuthority: OptionOrNullable<PublicKey>; newAccountFeeAuthority: OptionOrNullable<PublicKey>;  };


  export function getUpdateGlobalConfigInstructionDataSerializer(): Serializer<UpdateGlobalConfigInstructionDataArgs, UpdateGlobalConfigInstructionData> {
  return mapSerializer<UpdateGlobalConfigInstructionDataArgs, any, UpdateGlobalConfigInstructionData>(struct<UpdateGlobalConfigInstructionData>([['discriminator', bytes({ size: 8 })], ['newConfigAuthority', option(publicKeySerializer())], ['newAccountFeeAuthority', option(publicKeySerializer())]], { description: 'UpdateGlobalConfigInstructionData' }), (value) => ({ ...value, discriminator: new Uint8Array([164, 84, 130, 189, 111, 58, 250, 200]) }) ) as Serializer<UpdateGlobalConfigInstructionDataArgs, UpdateGlobalConfigInstructionData>;
}



  
  // Args.
      export type UpdateGlobalConfigInstructionArgs =           UpdateGlobalConfigInstructionDataArgs
      ;
  
// Instruction.
export function updateGlobalConfig(
  context: Pick<Context, "eddsa" | "identity" | "programs">,
                        input: UpdateGlobalConfigInstructionAccounts & UpdateGlobalConfigInstructionArgs,
      ): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey('gumballGuard', 'GGRDy4ieS7ExrUu313QkszyuT9o3BvDLuc3H5VLgCpSF');

  // Accounts.
  const resolvedAccounts = {
          authority: { index: 0, isWritable: false as boolean, value: input.authority ?? null },
          globalConfig: { index: 1, isWritable: true as boolean, value: input.globalConfig ?? null },
      } satisfies ResolvedAccountsWithIndices;

      // Arguments.
    const resolvedArgs: UpdateGlobalConfigInstructionArgs = { ...input };
  
    // Default values.
  if (!resolvedAccounts.authority.value) {
        resolvedAccounts.authority.value = context.identity;
      }
      if (!resolvedAccounts.globalConfig.value) {
        resolvedAccounts.globalConfig.value = findGlobalConfigPda(context);
      }
      
  // Accounts in order.
      const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a,b) => a.index - b.index);
  
  
  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
      const data = getUpdateGlobalConfigInstructionDataSerializer().serialize(resolvedArgs as UpdateGlobalConfigInstructionDataArgs);
  
  // Bytes Created On Chain.
      const bytesCreatedOnChain = 0;
  
  return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }]);
}
