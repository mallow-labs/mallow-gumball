/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGlobalConfigPda } from '../pdas';
import { GUMBALL_GUARD_PROGRAM_ADDRESS } from '../programs';

export const UPDATE_GLOBAL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([164, 84, 130, 189, 111, 58, 250, 200]);

export function getUpdateGlobalConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_GLOBAL_CONFIG_DISCRIMINATOR
  );
}

export type UpdateGlobalConfigInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGlobalConfig extends string
        ? WritableAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateGlobalConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  newConfigAuthority: Option<Address>;
  newAccountFeeAuthority: Option<Address>;
};

export type UpdateGlobalConfigInstructionDataArgs = {
  newConfigAuthority: OptionOrNullable<Address>;
  newAccountFeeAuthority: OptionOrNullable<Address>;
};

export function getUpdateGlobalConfigInstructionDataEncoder(): Encoder<UpdateGlobalConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newConfigAuthority', getOptionEncoder(getAddressEncoder())],
      ['newAccountFeeAuthority', getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_GLOBAL_CONFIG_DISCRIMINATOR })
  );
}

export function getUpdateGlobalConfigInstructionDataDecoder(): Decoder<UpdateGlobalConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newConfigAuthority', getOptionDecoder(getAddressDecoder())],
    ['newAccountFeeAuthority', getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getUpdateGlobalConfigInstructionDataCodec(): Codec<
  UpdateGlobalConfigInstructionDataArgs,
  UpdateGlobalConfigInstructionData
> {
  return combineCodec(
    getUpdateGlobalConfigInstructionDataEncoder(),
    getUpdateGlobalConfigInstructionDataDecoder()
  );
}

export type UpdateGlobalConfigAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
> = {
  /** The current config authority — must match global_config.config_authority. */
  authority: TransactionSigner<TAccountAuthority>;
  globalConfig?: Address<TAccountGlobalConfig>;
  newConfigAuthority: UpdateGlobalConfigInstructionDataArgs['newConfigAuthority'];
  newAccountFeeAuthority: UpdateGlobalConfigInstructionDataArgs['newAccountFeeAuthority'];
};

export async function getUpdateGlobalConfigInstructionAsync<
  TAccountAuthority extends string,
  TAccountGlobalConfig extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: UpdateGlobalConfigAsyncInput<TAccountAuthority, TAccountGlobalConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateGlobalConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('globalConfig', accounts.globalConfig),
    ],
    data: getUpdateGlobalConfigInstructionDataEncoder().encode(
      args as UpdateGlobalConfigInstructionDataArgs
    ),
    programAddress,
  } as UpdateGlobalConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig
  >);
}

export type UpdateGlobalConfigInput<
  TAccountAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
> = {
  /** The current config authority — must match global_config.config_authority. */
  authority: TransactionSigner<TAccountAuthority>;
  globalConfig: Address<TAccountGlobalConfig>;
  newConfigAuthority: UpdateGlobalConfigInstructionDataArgs['newConfigAuthority'];
  newAccountFeeAuthority: UpdateGlobalConfigInstructionDataArgs['newAccountFeeAuthority'];
};

export function getUpdateGlobalConfigInstruction<
  TAccountAuthority extends string,
  TAccountGlobalConfig extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: UpdateGlobalConfigInput<TAccountAuthority, TAccountGlobalConfig>,
  config?: { programAddress?: TProgramAddress }
): UpdateGlobalConfigInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountGlobalConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('globalConfig', accounts.globalConfig),
    ],
    data: getUpdateGlobalConfigInstructionDataEncoder().encode(
      args as UpdateGlobalConfigInstructionDataArgs
    ),
    programAddress,
  } as UpdateGlobalConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig
  >);
}

export type ParsedUpdateGlobalConfigInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The current config authority — must match global_config.config_authority. */
    authority: TAccountMetas[0];
    globalConfig: TAccountMetas[1];
  };
  data: UpdateGlobalConfigInstructionData;
};

export function parseUpdateGlobalConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateGlobalConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), globalConfig: getNextAccount() },
    data: getUpdateGlobalConfigInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
