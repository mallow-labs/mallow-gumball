/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findGlobalConfigPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateGlobalConfigInstructionAccounts = {
  /** The current config authority — must match global_config.config_authority. */
  authority?: Signer;
  globalConfig?: PublicKey | Pda;
};

// Data.
export type UpdateGlobalConfigInstructionData = {
  discriminator: Array<number>;
  newConfigAuthority: Option<PublicKey>;
  newAccountFeeAuthority: Option<PublicKey>;
};

export type UpdateGlobalConfigInstructionDataArgs = {
  newConfigAuthority: OptionOrNullable<PublicKey>;
  newAccountFeeAuthority: OptionOrNullable<PublicKey>;
};

export function getUpdateGlobalConfigInstructionDataSerializer(): Serializer<
  UpdateGlobalConfigInstructionDataArgs,
  UpdateGlobalConfigInstructionData
> {
  return mapSerializer<
    UpdateGlobalConfigInstructionDataArgs,
    any,
    UpdateGlobalConfigInstructionData
  >(
    struct<UpdateGlobalConfigInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['newConfigAuthority', option(publicKeySerializer())],
        ['newAccountFeeAuthority', option(publicKeySerializer())],
      ],
      { description: 'UpdateGlobalConfigInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [164, 84, 130, 189, 111, 58, 250, 200],
    })
  ) as Serializer<
    UpdateGlobalConfigInstructionDataArgs,
    UpdateGlobalConfigInstructionData
  >;
}

// Args.
export type UpdateGlobalConfigInstructionArgs =
  UpdateGlobalConfigInstructionDataArgs;

// Instruction.
export function updateGlobalConfig(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdateGlobalConfigInstructionAccounts &
    UpdateGlobalConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'gumballGuard',
    'GGRDy4ieS7ExrUu313QkszyuT9o3BvDLuc3H5VLgCpSF'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    authority: { index: 0, isWritable: false, value: input.authority ?? null },
    globalConfig: {
      index: 1,
      isWritable: true,
      value: input.globalConfig ?? null,
    },
  };

  // Arguments.
  const resolvedArgs: UpdateGlobalConfigInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.globalConfig.value) {
    resolvedAccounts.globalConfig.value = findGlobalConfigPda(context);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateGlobalConfigInstructionDataSerializer().serialize(
    resolvedArgs as UpdateGlobalConfigInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
