/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { GUMBALL_GUARD_PROGRAM_ADDRESS } from '../programs';

export const WRAP_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  178, 40, 10, 189, 228, 129, 186, 140,
]);

export function getWrapDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(WRAP_DISCRIMINATOR);
}

export type WrapInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountGumballGuard extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMachine extends string | AccountMeta<string> = string,
  TAccountMachineProgram extends string | AccountMeta<string> =
    'MGUMqztv7MHgoHBYWbvMyL3E3NJ4UHfTwgLJUQAbKGa',
  TAccountMachineAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballGuard extends string
        ? ReadonlyAccount<TAccountGumballGuard>
        : TAccountGumballGuard,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMachine extends string
        ? WritableAccount<TAccountMachine>
        : TAccountMachine,
      TAccountMachineProgram extends string
        ? ReadonlyAccount<TAccountMachineProgram>
        : TAccountMachineProgram,
      TAccountMachineAuthority extends string
        ? ReadonlySignerAccount<TAccountMachineAuthority> &
            AccountSignerMeta<TAccountMachineAuthority>
        : TAccountMachineAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type WrapInstructionData = { discriminator: ReadonlyUint8Array };

export type WrapInstructionDataArgs = {};

export function getWrapInstructionDataEncoder(): FixedSizeEncoder<WrapInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: WRAP_DISCRIMINATOR })
  );
}

export function getWrapInstructionDataDecoder(): FixedSizeDecoder<WrapInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getWrapInstructionDataCodec(): FixedSizeCodec<
  WrapInstructionDataArgs,
  WrapInstructionData
> {
  return combineCodec(
    getWrapInstructionDataEncoder(),
    getWrapInstructionDataDecoder()
  );
}

export type WrapInput<
  TAccountGumballGuard extends string = string,
  TAccountAuthority extends string = string,
  TAccountMachine extends string = string,
  TAccountMachineProgram extends string = string,
  TAccountMachineAuthority extends string = string,
> = {
  gumballGuard: Address<TAccountGumballGuard>;
  authority: TransactionSigner<TAccountAuthority>;
  machine: Address<TAccountMachine>;
  machineProgram?: Address<TAccountMachineProgram>;
  machineAuthority: TransactionSigner<TAccountMachineAuthority>;
};

export function getWrapInstruction<
  TAccountGumballGuard extends string,
  TAccountAuthority extends string,
  TAccountMachine extends string,
  TAccountMachineProgram extends string,
  TAccountMachineAuthority extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: WrapInput<
    TAccountGumballGuard,
    TAccountAuthority,
    TAccountMachine,
    TAccountMachineProgram,
    TAccountMachineAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): WrapInstruction<
  TProgramAddress,
  TAccountGumballGuard,
  TAccountAuthority,
  TAccountMachine,
  TAccountMachineProgram,
  TAccountMachineAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballGuard: { value: input.gumballGuard ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    machine: { value: input.machine ?? null, isWritable: true },
    machineProgram: { value: input.machineProgram ?? null, isWritable: false },
    machineAuthority: {
      value: input.machineAuthority ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.machineProgram.value) {
    accounts.machineProgram.value =
      'MGUMqztv7MHgoHBYWbvMyL3E3NJ4UHfTwgLJUQAbKGa' as Address<'MGUMqztv7MHgoHBYWbvMyL3E3NJ4UHfTwgLJUQAbKGa'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballGuard', accounts.gumballGuard),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('machine', accounts.machine),
      getAccountMeta('machineProgram', accounts.machineProgram),
      getAccountMeta('machineAuthority', accounts.machineAuthority),
    ],
    data: getWrapInstructionDataEncoder().encode({}),
    programAddress,
  } as WrapInstruction<
    TProgramAddress,
    TAccountGumballGuard,
    TAccountAuthority,
    TAccountMachine,
    TAccountMachineProgram,
    TAccountMachineAuthority
  >);
}

export type ParsedWrapInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    gumballGuard: TAccountMetas[0];
    authority: TAccountMetas[1];
    machine: TAccountMetas[2];
    machineProgram: TAccountMetas[3];
    machineAuthority: TAccountMetas[4];
  };
  data: WrapInstructionData;
};

export function parseWrapInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWrapInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gumballGuard: getNextAccount(),
      authority: getNextAccount(),
      machine: getNextAccount(),
      machineProgram: getNextAccount(),
      machineAuthority: getNextAccount(),
    },
    data: getWrapInstructionDataDecoder().decode(instruction.data),
  };
}
