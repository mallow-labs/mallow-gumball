/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGlobalConfigPda } from '../pdas';
import { GUMBALL_GUARD_PROGRAM_ADDRESS } from '../programs';

export const CREATE_GLOBAL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([47, 208, 62, 51, 32, 34, 119, 132]);

export function getCreateGlobalConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_GLOBAL_CONFIG_DISCRIMINATOR
  );
}

export type CreateGlobalConfigInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGlobalConfig extends string
        ? WritableAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateGlobalConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  configAuthority: Address;
  accountFeeAuthority: Address;
};

export type CreateGlobalConfigInstructionDataArgs = {
  configAuthority: Address;
  accountFeeAuthority: Address;
};

export function getCreateGlobalConfigInstructionDataEncoder(): FixedSizeEncoder<CreateGlobalConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['configAuthority', getAddressEncoder()],
      ['accountFeeAuthority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_GLOBAL_CONFIG_DISCRIMINATOR })
  );
}

export function getCreateGlobalConfigInstructionDataDecoder(): FixedSizeDecoder<CreateGlobalConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['configAuthority', getAddressDecoder()],
    ['accountFeeAuthority', getAddressDecoder()],
  ]);
}

export function getCreateGlobalConfigInstructionDataCodec(): FixedSizeCodec<
  CreateGlobalConfigInstructionDataArgs,
  CreateGlobalConfigInstructionData
> {
  return combineCodec(
    getCreateGlobalConfigInstructionDataEncoder(),
    getCreateGlobalConfigInstructionDataDecoder()
  );
}

export type CreateGlobalConfigAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  globalConfig?: Address<TAccountGlobalConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  configAuthority: CreateGlobalConfigInstructionDataArgs['configAuthority'];
  accountFeeAuthority: CreateGlobalConfigInstructionDataArgs['accountFeeAuthority'];
};

export async function getCreateGlobalConfigInstructionAsync<
  TAccountAuthority extends string,
  TAccountGlobalConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: CreateGlobalConfigAsyncInput<
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateGlobalConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('globalConfig', accounts.globalConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCreateGlobalConfigInstructionDataEncoder().encode(
      args as CreateGlobalConfigInstructionDataArgs
    ),
    programAddress,
  } as CreateGlobalConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >);
}

export type CreateGlobalConfigInput<
  TAccountAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  globalConfig: Address<TAccountGlobalConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  configAuthority: CreateGlobalConfigInstructionDataArgs['configAuthority'];
  accountFeeAuthority: CreateGlobalConfigInstructionDataArgs['accountFeeAuthority'];
};

export function getCreateGlobalConfigInstruction<
  TAccountAuthority extends string,
  TAccountGlobalConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: CreateGlobalConfigInput<
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateGlobalConfigInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountGlobalConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('globalConfig', accounts.globalConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCreateGlobalConfigInstructionDataEncoder().encode(
      args as CreateGlobalConfigInstructionDataArgs
    ),
    programAddress,
  } as CreateGlobalConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >);
}

export type ParsedCreateGlobalConfigInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    globalConfig: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateGlobalConfigInstructionData;
};

export function parseCreateGlobalConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateGlobalConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      globalConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateGlobalConfigInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
