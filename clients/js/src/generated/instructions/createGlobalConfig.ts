/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findGlobalConfigPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateGlobalConfigInstructionAccounts = {
  authority?: Signer;
  globalConfig?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateGlobalConfigInstructionData = {
  discriminator: Array<number>;
  configAuthority: PublicKey;
  accountFeeAuthority: PublicKey;
};

export type CreateGlobalConfigInstructionDataArgs = {
  configAuthority: PublicKey;
  accountFeeAuthority: PublicKey;
};

export function getCreateGlobalConfigInstructionDataSerializer(): Serializer<
  CreateGlobalConfigInstructionDataArgs,
  CreateGlobalConfigInstructionData
> {
  return mapSerializer<
    CreateGlobalConfigInstructionDataArgs,
    any,
    CreateGlobalConfigInstructionData
  >(
    struct<CreateGlobalConfigInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['configAuthority', publicKeySerializer()],
        ['accountFeeAuthority', publicKeySerializer()],
      ],
      { description: 'CreateGlobalConfigInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [47, 208, 62, 51, 32, 34, 119, 132],
    })
  ) as Serializer<
    CreateGlobalConfigInstructionDataArgs,
    CreateGlobalConfigInstructionData
  >;
}

// Args.
export type CreateGlobalConfigInstructionArgs =
  CreateGlobalConfigInstructionDataArgs;

// Instruction.
export function createGlobalConfig(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: CreateGlobalConfigInstructionAccounts &
    CreateGlobalConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'gumballGuard',
    'GGRDy4ieS7ExrUu313QkszyuT9o3BvDLuc3H5VLgCpSF'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    authority: { index: 0, isWritable: true, value: input.authority ?? null },
    globalConfig: {
      index: 1,
      isWritable: true,
      value: input.globalConfig ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null,
    },
  };

  // Arguments.
  const resolvedArgs: CreateGlobalConfigInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.globalConfig.value) {
    resolvedAccounts.globalConfig.value = findGlobalConfigPda(context);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateGlobalConfigInstructionDataSerializer().serialize(
    resolvedArgs as CreateGlobalConfigInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
