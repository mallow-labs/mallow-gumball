/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGumballMachineAuthorityPda } from '../../hooked';
import { findAddItemRequestPda } from '../pdas';
import { MALLOW_GUMBALL_PROGRAM_ADDRESS } from '../programs';

export const APPROVE_ADD_ITEM_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([135, 250, 51, 252, 70, 171, 19, 48]);

export function getApproveAddItemDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_ADD_ITEM_DISCRIMINATOR
  );
}

export type ApproveAddItemInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountGumballMachine extends string | AccountMeta<string> = string,
  TAccountAddItemRequest extends string | AccountMeta<string> = string,
  TAccountAuthorityPda extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSeller extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballMachine extends string
        ? WritableAccount<TAccountGumballMachine>
        : TAccountGumballMachine,
      TAccountAddItemRequest extends string
        ? WritableAccount<TAccountAddItemRequest>
        : TAccountAddItemRequest,
      TAccountAuthorityPda extends string
        ? WritableAccount<TAccountAuthorityPda>
        : TAccountAuthorityPda,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSeller extends string
        ? WritableAccount<TAccountSeller>
        : TAccountSeller,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveAddItemInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApproveAddItemInstructionDataArgs = {};

export function getApproveAddItemInstructionDataEncoder(): FixedSizeEncoder<ApproveAddItemInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: APPROVE_ADD_ITEM_DISCRIMINATOR })
  );
}

export function getApproveAddItemInstructionDataDecoder(): FixedSizeDecoder<ApproveAddItemInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApproveAddItemInstructionDataCodec(): FixedSizeCodec<
  ApproveAddItemInstructionDataArgs,
  ApproveAddItemInstructionData
> {
  return combineCodec(
    getApproveAddItemInstructionDataEncoder(),
    getApproveAddItemInstructionDataDecoder()
  );
}

export type ApproveAddItemAsyncInput<
  TAccountGumballMachine extends string = string,
  TAccountAddItemRequest extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountAuthority extends string = string,
  TAccountSeller extends string = string,
  TAccountAsset extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Gumball Machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Add item request account. */
  addItemRequest?: Address<TAccountAddItemRequest>;
  authorityPda?: Address<TAccountAuthorityPda>;
  /** Authority of the gumball machine. */
  authority: TransactionSigner<TAccountAuthority>;
  seller: Address<TAccountSeller>;
  asset: Address<TAccountAsset>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getApproveAddItemInstructionAsync<
  TAccountGumballMachine extends string,
  TAccountAddItemRequest extends string,
  TAccountAuthorityPda extends string,
  TAccountAuthority extends string,
  TAccountSeller extends string,
  TAccountAsset extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: ApproveAddItemAsyncInput<
    TAccountGumballMachine,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ApproveAddItemInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    addItemRequest: { value: input.addItemRequest ?? null, isWritable: true },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    seller: { value: input.seller ?? null, isWritable: true },
    asset: { value: input.asset ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.addItemRequest.value) {
    accounts.addItemRequest.value = await findAddItemRequestPda({
      asset: getAddressFromResolvedInstructionAccount(
        'asset',
        accounts.asset.value
      ),
    });
  }
  if (!accounts.authorityPda.value) {
    accounts.authorityPda.value = await findGumballMachineAuthorityPda({
      gumballMachine: getAddressFromResolvedInstructionAccount(
        'gumballMachine',
        accounts.gumballMachine.value
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('addItemRequest', accounts.addItemRequest),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('asset', accounts.asset),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getApproveAddItemInstructionDataEncoder().encode({}),
    programAddress,
  } as ApproveAddItemInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountSystemProgram
  >);
}

export type ApproveAddItemInput<
  TAccountGumballMachine extends string = string,
  TAccountAddItemRequest extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountAuthority extends string = string,
  TAccountSeller extends string = string,
  TAccountAsset extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Gumball Machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Add item request account. */
  addItemRequest: Address<TAccountAddItemRequest>;
  authorityPda: Address<TAccountAuthorityPda>;
  /** Authority of the gumball machine. */
  authority: TransactionSigner<TAccountAuthority>;
  seller: Address<TAccountSeller>;
  asset: Address<TAccountAsset>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getApproveAddItemInstruction<
  TAccountGumballMachine extends string,
  TAccountAddItemRequest extends string,
  TAccountAuthorityPda extends string,
  TAccountAuthority extends string,
  TAccountSeller extends string,
  TAccountAsset extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: ApproveAddItemInput<
    TAccountGumballMachine,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ApproveAddItemInstruction<
  TProgramAddress,
  TAccountGumballMachine,
  TAccountAddItemRequest,
  TAccountAuthorityPda,
  TAccountAuthority,
  TAccountSeller,
  TAccountAsset,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    addItemRequest: { value: input.addItemRequest ?? null, isWritable: true },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    seller: { value: input.seller ?? null, isWritable: true },
    asset: { value: input.asset ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('addItemRequest', accounts.addItemRequest),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('asset', accounts.asset),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getApproveAddItemInstructionDataEncoder().encode({}),
    programAddress,
  } as ApproveAddItemInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountAddItemRequest,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountSystemProgram
  >);
}

export type ParsedApproveAddItemInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Gumball Machine account. */
    gumballMachine: TAccountMetas[0];
    /** Add item request account. */
    addItemRequest: TAccountMetas[1];
    authorityPda: TAccountMetas[2];
    /** Authority of the gumball machine. */
    authority: TAccountMetas[3];
    seller: TAccountMetas[4];
    asset: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: ApproveAddItemInstructionData;
};

export function parseApproveAddItemInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApproveAddItemInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gumballMachine: getNextAccount(),
      addItemRequest: getNextAccount(),
      authorityPda: getNextAccount(),
      authority: getNextAccount(),
      seller: getNextAccount(),
      asset: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getApproveAddItemInstructionDataDecoder().decode(instruction.data),
  };
}
