/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findGlobalConfigPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseMintLimitInstructionAccounts = {
  /** The protocol authority — must match global_config.account_fee_authority and receives rent. */
  authority?: Signer;
  /** The GlobalConfig PDA — used to validate the authority. */
  globalConfig?: PublicKey | Pda;
  /** The gumball guard that this counter was created under. Must be closed (empty). */
  gumballGuard: PublicKey | Pda;
  /** The MintCounter PDA to close. */
  mintCounter: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CloseMintLimitInstructionData = { discriminator: Array<number> };

export type CloseMintLimitInstructionDataArgs = {};

export function getCloseMintLimitInstructionDataSerializer(): Serializer<
  CloseMintLimitInstructionDataArgs,
  CloseMintLimitInstructionData
> {
  return mapSerializer<
    CloseMintLimitInstructionDataArgs,
    any,
    CloseMintLimitInstructionData
  >(
    struct<CloseMintLimitInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'CloseMintLimitInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [234, 100, 43, 20, 76, 102, 26, 20],
    })
  ) as Serializer<
    CloseMintLimitInstructionDataArgs,
    CloseMintLimitInstructionData
  >;
}

// Instruction.
export function closeMintLimit(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: CloseMintLimitInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'gumballGuard',
    'GGRDy4ieS7ExrUu313QkszyuT9o3BvDLuc3H5VLgCpSF'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    authority: { index: 0, isWritable: true, value: input.authority ?? null },
    globalConfig: {
      index: 1,
      isWritable: false,
      value: input.globalConfig ?? null,
    },
    gumballGuard: {
      index: 2,
      isWritable: false,
      value: input.gumballGuard ?? null,
    },
    mintCounter: {
      index: 3,
      isWritable: true,
      value: input.mintCounter ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: input.systemProgram ?? null,
    },
  };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.globalConfig.value) {
    resolvedAccounts.globalConfig.value = findGlobalConfigPda(context);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseMintLimitInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
