/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGlobalConfigPda } from '../pdas';
import { GUMBALL_GUARD_PROGRAM_ADDRESS } from '../programs';

export const CLOSE_ALLOWLIST_PROOF_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([190, 213, 48, 188, 252, 208, 190, 242]);

export function getCloseAllowlistProofDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_ALLOWLIST_PROOF_DISCRIMINATOR
  );
}

export type CloseAllowlistProofInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountGumballGuard extends string | AccountMeta<string> = string,
  TAccountAllowListProof extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountGumballGuard extends string
        ? ReadonlyAccount<TAccountGumballGuard>
        : TAccountGumballGuard,
      TAccountAllowListProof extends string
        ? WritableAccount<TAccountAllowListProof>
        : TAccountAllowListProof,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseAllowlistProofInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseAllowlistProofInstructionDataArgs = {};

export function getCloseAllowlistProofInstructionDataEncoder(): FixedSizeEncoder<CloseAllowlistProofInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_ALLOWLIST_PROOF_DISCRIMINATOR,
    })
  );
}

export function getCloseAllowlistProofInstructionDataDecoder(): FixedSizeDecoder<CloseAllowlistProofInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseAllowlistProofInstructionDataCodec(): FixedSizeCodec<
  CloseAllowlistProofInstructionDataArgs,
  CloseAllowlistProofInstructionData
> {
  return combineCodec(
    getCloseAllowlistProofInstructionDataEncoder(),
    getCloseAllowlistProofInstructionDataDecoder()
  );
}

export type CloseAllowlistProofAsyncInput<
  TAccountAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountGumballGuard extends string = string,
  TAccountAllowListProof extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The protocol authority — must match global_config.account_fee_authority and receives rent. */
  authority: TransactionSigner<TAccountAuthority>;
  /** The GlobalConfig PDA — used to validate the authority. */
  globalConfig?: Address<TAccountGlobalConfig>;
  /** The gumball guard that this proof was created under. Must be closed (empty). */
  gumballGuard: Address<TAccountGumballGuard>;
  /** The AllowListProof PDA to close. */
  allowListProof: Address<TAccountAllowListProof>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCloseAllowlistProofInstructionAsync<
  TAccountAuthority extends string,
  TAccountGlobalConfig extends string,
  TAccountGumballGuard extends string,
  TAccountAllowListProof extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: CloseAllowlistProofAsyncInput<
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountGumballGuard,
    TAccountAllowListProof,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseAllowlistProofInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountGumballGuard,
    TAccountAllowListProof,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    gumballGuard: { value: input.gumballGuard ?? null, isWritable: false },
    allowListProof: { value: input.allowListProof ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('globalConfig', accounts.globalConfig),
      getAccountMeta('gumballGuard', accounts.gumballGuard),
      getAccountMeta('allowListProof', accounts.allowListProof),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCloseAllowlistProofInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseAllowlistProofInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountGumballGuard,
    TAccountAllowListProof,
    TAccountSystemProgram
  >);
}

export type CloseAllowlistProofInput<
  TAccountAuthority extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountGumballGuard extends string = string,
  TAccountAllowListProof extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The protocol authority — must match global_config.account_fee_authority and receives rent. */
  authority: TransactionSigner<TAccountAuthority>;
  /** The GlobalConfig PDA — used to validate the authority. */
  globalConfig: Address<TAccountGlobalConfig>;
  /** The gumball guard that this proof was created under. Must be closed (empty). */
  gumballGuard: Address<TAccountGumballGuard>;
  /** The AllowListProof PDA to close. */
  allowListProof: Address<TAccountAllowListProof>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCloseAllowlistProofInstruction<
  TAccountAuthority extends string,
  TAccountGlobalConfig extends string,
  TAccountGumballGuard extends string,
  TAccountAllowListProof extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
>(
  input: CloseAllowlistProofInput<
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountGumballGuard,
    TAccountAllowListProof,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CloseAllowlistProofInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountGlobalConfig,
  TAccountGumballGuard,
  TAccountAllowListProof,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GUMBALL_GUARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    gumballGuard: { value: input.gumballGuard ?? null, isWritable: false },
    allowListProof: { value: input.allowListProof ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('globalConfig', accounts.globalConfig),
      getAccountMeta('gumballGuard', accounts.gumballGuard),
      getAccountMeta('allowListProof', accounts.allowListProof),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCloseAllowlistProofInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseAllowlistProofInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountGlobalConfig,
    TAccountGumballGuard,
    TAccountAllowListProof,
    TAccountSystemProgram
  >);
}

export type ParsedCloseAllowlistProofInstruction<
  TProgram extends string = typeof GUMBALL_GUARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The protocol authority — must match global_config.account_fee_authority and receives rent. */
    authority: TAccountMetas[0];
    /** The GlobalConfig PDA — used to validate the authority. */
    globalConfig: TAccountMetas[1];
    /** The gumball guard that this proof was created under. Must be closed (empty). */
    gumballGuard: TAccountMetas[2];
    /** The AllowListProof PDA to close. */
    allowListProof: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CloseAllowlistProofInstructionData;
};

export function parseCloseAllowlistProofInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseAllowlistProofInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      globalConfig: getNextAccount(),
      gumballGuard: getNextAccount(),
      allowListProof: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseAllowlistProofInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
