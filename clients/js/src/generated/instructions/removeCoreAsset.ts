/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findGumballMachineAuthorityPda } from '../../hooked';
import { findSellerHistoryPda } from '../pdas';
import { MALLOW_GUMBALL_PROGRAM_ADDRESS } from '../programs';

export const REMOVE_CORE_ASSET_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([65, 17, 71, 132, 145, 88, 237, 166]);

export function getRemoveCoreAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_CORE_ASSET_DISCRIMINATOR
  );
}

export type RemoveCoreAssetInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountGumballMachine extends string | AccountMeta<string> = string,
  TAccountSellerHistory extends string | AccountMeta<string> = string,
  TAccountAuthorityPda extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSeller extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountCollection extends string | AccountMeta<string> = string,
  TAccountMplCoreProgram extends string | AccountMeta<string> =
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d',
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGumballMachine extends string
        ? WritableAccount<TAccountGumballMachine>
        : TAccountGumballMachine,
      TAccountSellerHistory extends string
        ? WritableAccount<TAccountSellerHistory>
        : TAccountSellerHistory,
      TAccountAuthorityPda extends string
        ? WritableAccount<TAccountAuthorityPda>
        : TAccountAuthorityPda,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSeller extends string
        ? WritableAccount<TAccountSeller>
        : TAccountSeller,
      TAccountAsset extends string
        ? WritableAccount<TAccountAsset>
        : TAccountAsset,
      TAccountCollection extends string
        ? WritableAccount<TAccountCollection>
        : TAccountCollection,
      TAccountMplCoreProgram extends string
        ? ReadonlyAccount<TAccountMplCoreProgram>
        : TAccountMplCoreProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveCoreAssetInstructionData = {
  discriminator: ReadonlyUint8Array;
  index: number;
};

export type RemoveCoreAssetInstructionDataArgs = { index: number };

export function getRemoveCoreAssetInstructionDataEncoder(): FixedSizeEncoder<RemoveCoreAssetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['index', getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_CORE_ASSET_DISCRIMINATOR })
  );
}

export function getRemoveCoreAssetInstructionDataDecoder(): FixedSizeDecoder<RemoveCoreAssetInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['index', getU32Decoder()],
  ]);
}

export function getRemoveCoreAssetInstructionDataCodec(): FixedSizeCodec<
  RemoveCoreAssetInstructionDataArgs,
  RemoveCoreAssetInstructionData
> {
  return combineCodec(
    getRemoveCoreAssetInstructionDataEncoder(),
    getRemoveCoreAssetInstructionDataDecoder()
  );
}

export type RemoveCoreAssetAsyncInput<
  TAccountGumballMachine extends string = string,
  TAccountSellerHistory extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountAuthority extends string = string,
  TAccountSeller extends string = string,
  TAccountAsset extends string = string,
  TAccountCollection extends string = string,
  TAccountMplCoreProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Gumball Machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Seller history account. */
  sellerHistory?: Address<TAccountSellerHistory>;
  authorityPda?: Address<TAccountAuthorityPda>;
  /** Seller of the asset. */
  authority: TransactionSigner<TAccountAuthority>;
  seller: Address<TAccountSeller>;
  asset: Address<TAccountAsset>;
  /** Core asset's collection if it's part of one. */
  collection?: Address<TAccountCollection>;
  mplCoreProgram?: Address<TAccountMplCoreProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  index: RemoveCoreAssetInstructionDataArgs['index'];
};

export async function getRemoveCoreAssetInstructionAsync<
  TAccountGumballMachine extends string,
  TAccountSellerHistory extends string,
  TAccountAuthorityPda extends string,
  TAccountAuthority extends string,
  TAccountSeller extends string,
  TAccountAsset extends string,
  TAccountCollection extends string,
  TAccountMplCoreProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: RemoveCoreAssetAsyncInput<
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountCollection,
    TAccountMplCoreProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RemoveCoreAssetInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountCollection,
    TAccountMplCoreProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    sellerHistory: { value: input.sellerHistory ?? null, isWritable: true },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    seller: { value: input.seller ?? null, isWritable: true },
    asset: { value: input.asset ?? null, isWritable: true },
    collection: { value: input.collection ?? null, isWritable: true },
    mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.sellerHistory.value) {
    accounts.sellerHistory.value = await findSellerHistoryPda({
      seller: getAddressFromResolvedInstructionAccount(
        'seller',
        accounts.seller.value
      ),
      gumballMachine: getAddressFromResolvedInstructionAccount(
        'gumballMachine',
        accounts.gumballMachine.value
      ),
    });
  }
  if (!accounts.authorityPda.value) {
    accounts.authorityPda.value = await findGumballMachineAuthorityPda({
      gumballMachine: getAddressFromResolvedInstructionAccount(
        'gumballMachine',
        accounts.gumballMachine.value
      ),
    });
  }
  if (!accounts.mplCoreProgram.value) {
    accounts.mplCoreProgram.value =
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d' as Address<'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('sellerHistory', accounts.sellerHistory),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('asset', accounts.asset),
      getAccountMeta('collection', accounts.collection),
      getAccountMeta('mplCoreProgram', accounts.mplCoreProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRemoveCoreAssetInstructionDataEncoder().encode(
      args as RemoveCoreAssetInstructionDataArgs
    ),
    programAddress,
  } as RemoveCoreAssetInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountCollection,
    TAccountMplCoreProgram,
    TAccountSystemProgram
  >);
}

export type RemoveCoreAssetInput<
  TAccountGumballMachine extends string = string,
  TAccountSellerHistory extends string = string,
  TAccountAuthorityPda extends string = string,
  TAccountAuthority extends string = string,
  TAccountSeller extends string = string,
  TAccountAsset extends string = string,
  TAccountCollection extends string = string,
  TAccountMplCoreProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Gumball Machine account. */
  gumballMachine: Address<TAccountGumballMachine>;
  /** Seller history account. */
  sellerHistory: Address<TAccountSellerHistory>;
  authorityPda: Address<TAccountAuthorityPda>;
  /** Seller of the asset. */
  authority: TransactionSigner<TAccountAuthority>;
  seller: Address<TAccountSeller>;
  asset: Address<TAccountAsset>;
  /** Core asset's collection if it's part of one. */
  collection?: Address<TAccountCollection>;
  mplCoreProgram?: Address<TAccountMplCoreProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  index: RemoveCoreAssetInstructionDataArgs['index'];
};

export function getRemoveCoreAssetInstruction<
  TAccountGumballMachine extends string,
  TAccountSellerHistory extends string,
  TAccountAuthorityPda extends string,
  TAccountAuthority extends string,
  TAccountSeller extends string,
  TAccountAsset extends string,
  TAccountCollection extends string,
  TAccountMplCoreProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
>(
  input: RemoveCoreAssetInput<
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountCollection,
    TAccountMplCoreProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RemoveCoreAssetInstruction<
  TProgramAddress,
  TAccountGumballMachine,
  TAccountSellerHistory,
  TAccountAuthorityPda,
  TAccountAuthority,
  TAccountSeller,
  TAccountAsset,
  TAccountCollection,
  TAccountMplCoreProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MALLOW_GUMBALL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gumballMachine: { value: input.gumballMachine ?? null, isWritable: true },
    sellerHistory: { value: input.sellerHistory ?? null, isWritable: true },
    authorityPda: { value: input.authorityPda ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    seller: { value: input.seller ?? null, isWritable: true },
    asset: { value: input.asset ?? null, isWritable: true },
    collection: { value: input.collection ?? null, isWritable: true },
    mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.mplCoreProgram.value) {
    accounts.mplCoreProgram.value =
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d' as Address<'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('gumballMachine', accounts.gumballMachine),
      getAccountMeta('sellerHistory', accounts.sellerHistory),
      getAccountMeta('authorityPda', accounts.authorityPda),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('asset', accounts.asset),
      getAccountMeta('collection', accounts.collection),
      getAccountMeta('mplCoreProgram', accounts.mplCoreProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRemoveCoreAssetInstructionDataEncoder().encode(
      args as RemoveCoreAssetInstructionDataArgs
    ),
    programAddress,
  } as RemoveCoreAssetInstruction<
    TProgramAddress,
    TAccountGumballMachine,
    TAccountSellerHistory,
    TAccountAuthorityPda,
    TAccountAuthority,
    TAccountSeller,
    TAccountAsset,
    TAccountCollection,
    TAccountMplCoreProgram,
    TAccountSystemProgram
  >);
}

export type ParsedRemoveCoreAssetInstruction<
  TProgram extends string = typeof MALLOW_GUMBALL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Gumball Machine account. */
    gumballMachine: TAccountMetas[0];
    /** Seller history account. */
    sellerHistory: TAccountMetas[1];
    authorityPda: TAccountMetas[2];
    /** Seller of the asset. */
    authority: TAccountMetas[3];
    seller: TAccountMetas[4];
    asset: TAccountMetas[5];
    /** Core asset's collection if it's part of one. */
    collection?: TAccountMetas[6] | undefined;
    mplCoreProgram: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: RemoveCoreAssetInstructionData;
};

export function parseRemoveCoreAssetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRemoveCoreAssetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === MALLOW_GUMBALL_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gumballMachine: getNextAccount(),
      sellerHistory: getNextAccount(),
      authorityPda: getNextAccount(),
      authority: getNextAccount(),
      seller: getNextAccount(),
      asset: getNextAccount(),
      collection: getNextOptionalAccount(),
      mplCoreProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRemoveCoreAssetInstructionDataDecoder().decode(instruction.data),
  };
}
