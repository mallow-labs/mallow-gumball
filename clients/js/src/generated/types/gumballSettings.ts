/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/kit';

export type GumballSettings = {
  /** Uri of off-chain metadata, max length 196 */
  uri: string;
  /** Number of assets that can be added. */
  itemCapacity: bigint;
  /** Max number of items that can be added by a single seller. */
  itemsPerSeller: number;
  /** Merkle root hash for sellers who can add items to the machine. */
  sellersMerkleRoot: Option<ReadonlyUint8Array>;
  /** Fee basis points paid to the machine authority. */
  curatorFeeBps: number;
  /** True if the front end should hide items that have been sold. */
  hideSoldItems: boolean;
  /** Payment token for the mint */
  paymentMint: Address;
};

export type GumballSettingsArgs = {
  /** Uri of off-chain metadata, max length 196 */
  uri: string;
  /** Number of assets that can be added. */
  itemCapacity: number | bigint;
  /** Max number of items that can be added by a single seller. */
  itemsPerSeller: number;
  /** Merkle root hash for sellers who can add items to the machine. */
  sellersMerkleRoot: OptionOrNullable<ReadonlyUint8Array>;
  /** Fee basis points paid to the machine authority. */
  curatorFeeBps: number;
  /** True if the front end should hide items that have been sold. */
  hideSoldItems: boolean;
  /** Payment token for the mint */
  paymentMint: Address;
};

export function getGumballSettingsEncoder(): Encoder<GumballSettingsArgs> {
  return getStructEncoder([
    ['uri', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ['itemCapacity', getU64Encoder()],
    ['itemsPerSeller', getU16Encoder()],
    [
      'sellersMerkleRoot',
      getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32)),
    ],
    ['curatorFeeBps', getU16Encoder()],
    ['hideSoldItems', getBooleanEncoder()],
    ['paymentMint', getAddressEncoder()],
  ]);
}

export function getGumballSettingsDecoder(): Decoder<GumballSettings> {
  return getStructDecoder([
    ['uri', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['itemCapacity', getU64Decoder()],
    ['itemsPerSeller', getU16Decoder()],
    [
      'sellersMerkleRoot',
      getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32)),
    ],
    ['curatorFeeBps', getU16Decoder()],
    ['hideSoldItems', getBooleanDecoder()],
    ['paymentMint', getAddressDecoder()],
  ]);
}

export function getGumballSettingsCodec(): Codec<
  GumballSettingsArgs,
  GumballSettings
> {
  return combineCodec(getGumballSettingsEncoder(), getGumballSettingsDecoder());
}
