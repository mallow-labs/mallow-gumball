/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getCnftCreatorDecoder,
  getCnftCreatorEncoder,
  type CnftCreator,
  type CnftCreatorArgs,
} from '.';

/**
 * Leaf parameters shared by all compressed-NFT instructions.
 *
 * Everything here is verified against the on-chain merkle root by the Bubblegum
 * V1 `Transfer` CPI: a lie about `seller_fee_basis_points` changes `data_hash`
 * and a lie about `creators` changes `creator_hash`, either of which makes the
 * proof fail. This is what makes royalties trustless without server trust.
 */
export type CnftArgs = {
  /** Current merkle root (fetched fresh from DAS at build time). */
  root: ReadonlyUint8Array;
  /**
   * Inner `keccak(borsh(MetadataArgs))` supplied from DAS. `data_hash` is
   * recomputed on-chain as `keccak(meta_hash ‖ sfbp_le)`.
   */
  metaHash: ReadonlyUint8Array;
  /** Seller fee basis points, folded into `data_hash`. */
  sellerFeeBasisPoints: number;
  /** Creators, folded into `creator_hash` and used for royalty payouts. */
  creators: Array<CnftCreator>;
  /** Leaf nonce. Binds the asset id: `asset_id = PDA(["asset", tree, nonce_le])`. */
  nonce: bigint;
  /** Leaf index within the merkle tree. */
  index: number;
  /**
   * Bubblegum leaf version. Only V1 (`1`) is supported for on-chain trading;
   * V2 leaves are rejected (see `assert_cnft_v1`).
   */
  version: number;
};

export type CnftArgsArgs = {
  /** Current merkle root (fetched fresh from DAS at build time). */
  root: ReadonlyUint8Array;
  /**
   * Inner `keccak(borsh(MetadataArgs))` supplied from DAS. `data_hash` is
   * recomputed on-chain as `keccak(meta_hash ‖ sfbp_le)`.
   */
  metaHash: ReadonlyUint8Array;
  /** Seller fee basis points, folded into `data_hash`. */
  sellerFeeBasisPoints: number;
  /** Creators, folded into `creator_hash` and used for royalty payouts. */
  creators: Array<CnftCreatorArgs>;
  /** Leaf nonce. Binds the asset id: `asset_id = PDA(["asset", tree, nonce_le])`. */
  nonce: number | bigint;
  /** Leaf index within the merkle tree. */
  index: number;
  /**
   * Bubblegum leaf version. Only V1 (`1`) is supported for on-chain trading;
   * V2 leaves are rejected (see `assert_cnft_v1`).
   */
  version: number;
};

export function getCnftArgsEncoder(): Encoder<CnftArgsArgs> {
  return getStructEncoder([
    ['root', fixEncoderSize(getBytesEncoder(), 32)],
    ['metaHash', fixEncoderSize(getBytesEncoder(), 32)],
    ['sellerFeeBasisPoints', getU16Encoder()],
    ['creators', getArrayEncoder(getCnftCreatorEncoder())],
    ['nonce', getU64Encoder()],
    ['index', getU32Encoder()],
    ['version', getU8Encoder()],
  ]);
}

export function getCnftArgsDecoder(): Decoder<CnftArgs> {
  return getStructDecoder([
    ['root', fixDecoderSize(getBytesDecoder(), 32)],
    ['metaHash', fixDecoderSize(getBytesDecoder(), 32)],
    ['sellerFeeBasisPoints', getU16Decoder()],
    ['creators', getArrayDecoder(getCnftCreatorDecoder())],
    ['nonce', getU64Decoder()],
    ['index', getU32Decoder()],
    ['version', getU8Decoder()],
  ]);
}

export function getCnftArgsCodec(): Codec<CnftArgsArgs, CnftArgs> {
  return combineCodec(getCnftArgsEncoder(), getCnftArgsDecoder());
}
