/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';

/**
 * Guard that charges another NFT (token) from a specific collection as payment
 * for the mint.
 *
 * List of accounts required:
 *
 * 0. `[writeable]` Token account of the NFT.
 * 1. `[writeable]` Metadata account of the NFT.
 * 2. `[]` Mint account of the NFT.
 * 3. `[]` Account to receive the NFT.
 * 4. `[writeable]` Destination PDA key (seeds [destination pubkey, token program id, nft mint pubkey]).
 * 5. `[]` spl-associate-token program ID.
 * 6. `[]` Master edition (pNFT)
 * 7. `[writable]` Owner token record (pNFT)
 * 8. `[writable]` Destination token record (pNFT)
 * 9. `[]` Token Authorization Rules program (pNFT)
 * 10. `[]` Token Authorization Rules account (pNFT)
 */
export type NftPayment = { requiredCollection: Address; destination: Address };

export type NftPaymentArgs = NftPayment;

export function getNftPaymentEncoder(): FixedSizeEncoder<NftPaymentArgs> {
  return getStructEncoder([
    ['requiredCollection', getAddressEncoder()],
    ['destination', getAddressEncoder()],
  ]);
}

export function getNftPaymentDecoder(): FixedSizeDecoder<NftPayment> {
  return getStructDecoder([
    ['requiredCollection', getAddressDecoder()],
    ['destination', getAddressDecoder()],
  ]);
}

export function getNftPaymentCodec(): FixedSizeCodec<
  NftPaymentArgs,
  NftPayment
> {
  return combineCodec(getNftPaymentEncoder(), getNftPaymentDecoder());
}
