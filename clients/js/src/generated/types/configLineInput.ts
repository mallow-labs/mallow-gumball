/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';

/** Config line struct for storing asset (NFT) data pre-mint. */
export type ConfigLineInput = {
  /** Mint account of the asset. */
  mint: Address;
  /** Wallet that submitted the asset for sale. */
  seller: Address;
};

export type ConfigLineInputArgs = ConfigLineInput;

export function getConfigLineInputEncoder(): FixedSizeEncoder<ConfigLineInputArgs> {
  return getStructEncoder([
    ['mint', getAddressEncoder()],
    ['seller', getAddressEncoder()],
  ]);
}

export function getConfigLineInputDecoder(): FixedSizeDecoder<ConfigLineInput> {
  return getStructDecoder([
    ['mint', getAddressDecoder()],
    ['seller', getAddressDecoder()],
  ]);
}

export function getConfigLineInputCodec(): FixedSizeCodec<
  ConfigLineInputArgs,
  ConfigLineInput
> {
  return combineCodec(getConfigLineInputEncoder(), getConfigLineInputDecoder());
}
