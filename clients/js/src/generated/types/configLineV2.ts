/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';
import {
  getTokenStandardDecoder,
  getTokenStandardEncoder,
  type TokenStandard,
  type TokenStandardArgs,
} from '.';

/** Config line struct for storing asset data. */
export type ConfigLineV2 = {
  /** Mint account of the asset. */
  mint: Address;
  /** Wallet that submitted the asset for sale. */
  seller: Address;
  /** Wallet that will receive the asset upon sale. Empty until drawn. */
  buyer: Address;
  /** Token standard. */
  tokenStandard: TokenStandard;
  /** Amount of the asset. */
  amount: bigint;
};

export type ConfigLineV2Args = {
  /** Mint account of the asset. */
  mint: Address;
  /** Wallet that submitted the asset for sale. */
  seller: Address;
  /** Wallet that will receive the asset upon sale. Empty until drawn. */
  buyer: Address;
  /** Token standard. */
  tokenStandard: TokenStandardArgs;
  /** Amount of the asset. */
  amount: number | bigint;
};

export function getConfigLineV2Encoder(): FixedSizeEncoder<ConfigLineV2Args> {
  return getStructEncoder([
    ['mint', getAddressEncoder()],
    ['seller', getAddressEncoder()],
    ['buyer', getAddressEncoder()],
    ['tokenStandard', getTokenStandardEncoder()],
    ['amount', getU64Encoder()],
  ]);
}

export function getConfigLineV2Decoder(): FixedSizeDecoder<ConfigLineV2> {
  return getStructDecoder([
    ['mint', getAddressDecoder()],
    ['seller', getAddressDecoder()],
    ['buyer', getAddressDecoder()],
    ['tokenStandard', getTokenStandardDecoder()],
    ['amount', getU64Decoder()],
  ]);
}

export function getConfigLineV2Codec(): FixedSizeCodec<
  ConfigLineV2Args,
  ConfigLineV2
> {
  return combineCodec(getConfigLineV2Encoder(), getConfigLineV2Decoder());
}
