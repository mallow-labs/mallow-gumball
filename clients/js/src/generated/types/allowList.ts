/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

/**
 * Guard that uses a merkle tree to specify the addresses allowed to mint.
 *
 * List of accounts required:
 *
 * 0. `[]` Pda created by the merkle proof instruction (seeds `["allow_list", merke tree root,
 * payer key, gumball guard pubkey, gumball machine pubkey]`).
 */
export type AllowList = {
  /** Merkle root of the addresses allowed to mint. */
  merkleRoot: ReadonlyUint8Array;
};

export type AllowListArgs = AllowList;

export function getAllowListEncoder(): FixedSizeEncoder<AllowListArgs> {
  return getStructEncoder([
    ['merkleRoot', fixEncoderSize(getBytesEncoder(), 32)],
  ]);
}

export function getAllowListDecoder(): FixedSizeDecoder<AllowList> {
  return getStructDecoder([
    ['merkleRoot', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getAllowListCodec(): FixedSizeCodec<AllowListArgs, AllowList> {
  return combineCodec(getAllowListEncoder(), getAllowListDecoder());
}
