/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';
import {
  getTokenStandardDecoder,
  getTokenStandardEncoder,
  type TokenStandard,
  type TokenStandardArgs,
} from '.';

/** Config line struct for storing asset data. */
export type ConfigLine = {
  /** Mint account of the asset. */
  mint: Address;
  /** Wallet that submitted the asset for sale. */
  seller: Address;
  /** Wallet that will receive the asset upon sale. Empty until drawn. */
  buyer: Address;
  /** Token standard. */
  tokenStandard: TokenStandard;
};

export type ConfigLineArgs = {
  /** Mint account of the asset. */
  mint: Address;
  /** Wallet that submitted the asset for sale. */
  seller: Address;
  /** Wallet that will receive the asset upon sale. Empty until drawn. */
  buyer: Address;
  /** Token standard. */
  tokenStandard: TokenStandardArgs;
};

export function getConfigLineEncoder(): FixedSizeEncoder<ConfigLineArgs> {
  return getStructEncoder([
    ['mint', getAddressEncoder()],
    ['seller', getAddressEncoder()],
    ['buyer', getAddressEncoder()],
    ['tokenStandard', getTokenStandardEncoder()],
  ]);
}

export function getConfigLineDecoder(): FixedSizeDecoder<ConfigLine> {
  return getStructDecoder([
    ['mint', getAddressDecoder()],
    ['seller', getAddressDecoder()],
    ['buyer', getAddressDecoder()],
    ['tokenStandard', getTokenStandardDecoder()],
  ]);
}

export function getConfigLineCodec(): FixedSizeCodec<
  ConfigLineArgs,
  ConfigLine
> {
  return combineCodec(getConfigLineEncoder(), getConfigLineDecoder());
}
