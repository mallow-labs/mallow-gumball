/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getBooleanDecoder,
  getBooleanEncoder,
  getLamportsDecoder,
  getLamportsEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Lamports,
} from '@solana/kit';

/**
 * Guard is used to:
 * * charge a penalty for invalid transactions
 * * validate that the mint transaction is the last transaction
 * * verify that only authorized programs have instructions
 *
 * The `bot_tax` is applied to any error that occurs during the
 * validation of the guards.
 */
export type BotTax = { lamports: Lamports; lastInstruction: boolean };

export type BotTaxArgs = BotTax;

export function getBotTaxEncoder(): FixedSizeEncoder<BotTaxArgs> {
  return getStructEncoder([
    ['lamports', getLamportsEncoder(getU64Encoder())],
    ['lastInstruction', getBooleanEncoder()],
  ]);
}

export function getBotTaxDecoder(): FixedSizeDecoder<BotTax> {
  return getStructDecoder([
    ['lamports', getLamportsDecoder(getU64Decoder())],
    ['lastInstruction', getBooleanDecoder()],
  ]);
}

export function getBotTaxCodec(): FixedSizeCodec<BotTaxArgs, BotTax> {
  return combineCodec(getBotTaxEncoder(), getBotTaxDecoder());
}
