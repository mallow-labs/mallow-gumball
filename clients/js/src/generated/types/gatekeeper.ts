/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';

/**
 * Guard that validates if the payer of the transaction has a token from a specified
 * gateway network — in most cases, a token after completing a captcha challenge.
 *
 * List of accounts required:
 *
 * 0. `[writeable]` Gatekeeper token account.
 * 1. `[]` Gatekeeper program account.
 * 2. `[]` Gatekeeper expire account.
 */
export type Gatekeeper = {
  /** The network for the gateway token required */
  gatekeeperNetwork: Address;
  /**
   * Whether or not the token should expire after minting.
   * The gatekeeper network must support this if true.
   */
  expireOnUse: boolean;
};

export type GatekeeperArgs = Gatekeeper;

export function getGatekeeperEncoder(): FixedSizeEncoder<GatekeeperArgs> {
  return getStructEncoder([
    ['gatekeeperNetwork', getAddressEncoder()],
    ['expireOnUse', getBooleanEncoder()],
  ]);
}

export function getGatekeeperDecoder(): FixedSizeDecoder<Gatekeeper> {
  return getStructDecoder([
    ['gatekeeperNetwork', getAddressDecoder()],
    ['expireOnUse', getBooleanDecoder()],
  ]);
}

export function getGatekeeperCodec(): FixedSizeCodec<
  GatekeeperArgs,
  Gatekeeper
> {
  return combineCodec(getGatekeeperEncoder(), getGatekeeperDecoder());
}
