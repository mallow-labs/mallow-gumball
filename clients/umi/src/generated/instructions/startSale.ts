/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type StartSaleInstructionAccounts = {
  /** Gumball machine account. */
  gumballMachine: PublicKey | Pda;
  /** Gumball Machine authority. This can be the mint authority or the authority. */
  authority?: Signer;
};

// Data.
export type StartSaleInstructionData = { discriminator: Uint8Array };

export type StartSaleInstructionDataArgs = {};

export function getStartSaleInstructionDataSerializer(): Serializer<
  StartSaleInstructionDataArgs,
  StartSaleInstructionData
> {
  return mapSerializer<
    StartSaleInstructionDataArgs,
    any,
    StartSaleInstructionData
  >(
    struct<StartSaleInstructionData>([['discriminator', bytes({ size: 8 })]], {
      description: 'StartSaleInstructionData',
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([130, 69, 235, 113, 173, 219, 48, 228]),
    })
  ) as Serializer<StartSaleInstructionDataArgs, StartSaleInstructionData>;
}

// Instruction.
export function startSale(
  context: Pick<Context, 'identity' | 'programs'>,
  input: StartSaleInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mallowGumball',
    'MGUMqztv7MHgoHBYWbvMyL3E3NJ4UHfTwgLJUQAbKGa'
  );

  // Accounts.
  const resolvedAccounts = {
    gumballMachine: {
      index: 0,
      isWritable: true as boolean,
      value: input.gumballMachine ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getStartSaleInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
