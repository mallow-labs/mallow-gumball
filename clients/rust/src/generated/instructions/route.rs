//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::GuardType;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const ROUTE_DISCRIMINATOR: [u8; 8] = [229, 23, 203, 151, 122, 227, 173, 42];

/// Accounts.
#[derive(Debug)]
pub struct Route {
    pub gumball_guard: solana_address::Address,

    pub machine: solana_address::Address,

    pub payer: solana_address::Address,
}

impl Route {
    pub fn instruction(&self, args: RouteInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: RouteInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.gumball_guard,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.machine, false));
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = RouteInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::GUMBALL_GUARD_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct RouteInstructionData {
    discriminator: [u8; 8],
}

impl RouteInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [229, 23, 203, 151, 122, 227, 173, 42],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for RouteInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct RouteInstructionArgs {
    pub guard: GuardType,
    pub data: Vec<u8>,
    pub group: Option<String>,
}

impl RouteInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `Route`.
///
/// ### Accounts:
///
///   0. `[]` gumball_guard
///   1. `[writable]` machine
///   2. `[writable, signer]` payer
#[derive(Clone, Debug, Default)]
pub struct RouteBuilder {
    gumball_guard: Option<solana_address::Address>,
    machine: Option<solana_address::Address>,
    payer: Option<solana_address::Address>,
    guard: Option<GuardType>,
    data: Option<Vec<u8>>,
    group: Option<String>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl RouteBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn gumball_guard(&mut self, gumball_guard: solana_address::Address) -> &mut Self {
        self.gumball_guard = Some(gumball_guard);
        self
    }
    #[inline(always)]
    pub fn machine(&mut self, machine: solana_address::Address) -> &mut Self {
        self.machine = Some(machine);
        self
    }
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_address::Address) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn guard(&mut self, guard: GuardType) -> &mut Self {
        self.guard = Some(guard);
        self
    }
    #[inline(always)]
    pub fn data(&mut self, data: Vec<u8>) -> &mut Self {
        self.data = Some(data);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn group(&mut self, group: String) -> &mut Self {
        self.group = Some(group);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = Route {
            gumball_guard: self.gumball_guard.expect("gumball_guard is not set"),
            machine: self.machine.expect("machine is not set"),
            payer: self.payer.expect("payer is not set"),
        };
        let args = RouteInstructionArgs {
            guard: self.guard.clone().expect("guard is not set"),
            data: self.data.clone().expect("data is not set"),
            group: self.group.clone(),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `route` CPI accounts.
pub struct RouteCpiAccounts<'a, 'b> {
    pub gumball_guard: &'b solana_account_info::AccountInfo<'a>,

    pub machine: &'b solana_account_info::AccountInfo<'a>,

    pub payer: &'b solana_account_info::AccountInfo<'a>,
}

/// `route` CPI instruction.
pub struct RouteCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub gumball_guard: &'b solana_account_info::AccountInfo<'a>,

    pub machine: &'b solana_account_info::AccountInfo<'a>,

    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: RouteInstructionArgs,
}

impl<'a, 'b> RouteCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: RouteCpiAccounts<'a, 'b>,
        args: RouteInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            gumball_guard: accounts.gumball_guard,
            machine: accounts.machine,
            payer: accounts.payer,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.gumball_guard.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.machine.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = RouteInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::GUMBALL_GUARD_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.gumball_guard.clone());
        account_infos.push(self.machine.clone());
        account_infos.push(self.payer.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `Route` via CPI.
///
/// ### Accounts:
///
///   0. `[]` gumball_guard
///   1. `[writable]` machine
///   2. `[writable, signer]` payer
#[derive(Clone, Debug)]
pub struct RouteCpiBuilder<'a, 'b> {
    instruction: Box<RouteCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RouteCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RouteCpiBuilderInstruction {
            __program: program,
            gumball_guard: None,
            machine: None,
            payer: None,
            guard: None,
            data: None,
            group: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn gumball_guard(
        &mut self,
        gumball_guard: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.gumball_guard = Some(gumball_guard);
        self
    }
    #[inline(always)]
    pub fn machine(&mut self, machine: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.machine = Some(machine);
        self
    }
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn guard(&mut self, guard: GuardType) -> &mut Self {
        self.instruction.guard = Some(guard);
        self
    }
    #[inline(always)]
    pub fn data(&mut self, data: Vec<u8>) -> &mut Self {
        self.instruction.data = Some(data);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn group(&mut self, group: String) -> &mut Self {
        self.instruction.group = Some(group);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = RouteInstructionArgs {
            guard: self.instruction.guard.clone().expect("guard is not set"),
            data: self.instruction.data.clone().expect("data is not set"),
            group: self.instruction.group.clone(),
        };
        let instruction = RouteCpi {
            __program: self.instruction.__program,

            gumball_guard: self
                .instruction
                .gumball_guard
                .expect("gumball_guard is not set"),

            machine: self.instruction.machine.expect("machine is not set"),

            payer: self.instruction.payer.expect("payer is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct RouteCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    gumball_guard: Option<&'b solana_account_info::AccountInfo<'a>>,
    machine: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    guard: Option<GuardType>,
    data: Option<Vec<u8>>,
    group: Option<String>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
