//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLOSE_ALLOWLIST_PROOF_DISCRIMINATOR: [u8; 8] = [190, 213, 48, 188, 252, 208, 190, 242];

/// Accounts.
#[derive(Debug)]
pub struct CloseAllowlistProof {
    /// The protocol authority — must match global_config.account_fee_authority and receives rent.
    pub authority: solana_address::Address,
    /// The GlobalConfig PDA — used to validate the authority.
    pub global_config: solana_address::Address,
    /// The gumball guard that this proof was created under. Must be closed (empty).
    pub gumball_guard: solana_address::Address,
    /// The AllowListProof PDA to close.
    pub allow_list_proof: solana_address::Address,

    pub system_program: solana_address::Address,
}

impl CloseAllowlistProof {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.authority, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.global_config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.gumball_guard,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.allow_list_proof,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = CloseAllowlistProofInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::GUMBALL_GUARD_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CloseAllowlistProofInstructionData {
    discriminator: [u8; 8],
}

impl CloseAllowlistProofInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [190, 213, 48, 188, 252, 208, 190, 242],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CloseAllowlistProofInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `CloseAllowlistProof`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[]` global_config
///   2. `[]` gumball_guard
///   3. `[writable]` allow_list_proof
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct CloseAllowlistProofBuilder {
    authority: Option<solana_address::Address>,
    global_config: Option<solana_address::Address>,
    gumball_guard: Option<solana_address::Address>,
    allow_list_proof: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CloseAllowlistProofBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The protocol authority — must match global_config.account_fee_authority and receives rent.
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    /// The GlobalConfig PDA — used to validate the authority.
    #[inline(always)]
    pub fn global_config(&mut self, global_config: solana_address::Address) -> &mut Self {
        self.global_config = Some(global_config);
        self
    }
    /// The gumball guard that this proof was created under. Must be closed (empty).
    #[inline(always)]
    pub fn gumball_guard(&mut self, gumball_guard: solana_address::Address) -> &mut Self {
        self.gumball_guard = Some(gumball_guard);
        self
    }
    /// The AllowListProof PDA to close.
    #[inline(always)]
    pub fn allow_list_proof(&mut self, allow_list_proof: solana_address::Address) -> &mut Self {
        self.allow_list_proof = Some(allow_list_proof);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CloseAllowlistProof {
            authority: self.authority.expect("authority is not set"),
            global_config: self.global_config.expect("global_config is not set"),
            gumball_guard: self.gumball_guard.expect("gumball_guard is not set"),
            allow_list_proof: self.allow_list_proof.expect("allow_list_proof is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `close_allowlist_proof` CPI accounts.
pub struct CloseAllowlistProofCpiAccounts<'a, 'b> {
    /// The protocol authority — must match global_config.account_fee_authority and receives rent.
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// The GlobalConfig PDA — used to validate the authority.
    pub global_config: &'b solana_account_info::AccountInfo<'a>,
    /// The gumball guard that this proof was created under. Must be closed (empty).
    pub gumball_guard: &'b solana_account_info::AccountInfo<'a>,
    /// The AllowListProof PDA to close.
    pub allow_list_proof: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `close_allowlist_proof` CPI instruction.
pub struct CloseAllowlistProofCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The protocol authority — must match global_config.account_fee_authority and receives rent.
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// The GlobalConfig PDA — used to validate the authority.
    pub global_config: &'b solana_account_info::AccountInfo<'a>,
    /// The gumball guard that this proof was created under. Must be closed (empty).
    pub gumball_guard: &'b solana_account_info::AccountInfo<'a>,
    /// The AllowListProof PDA to close.
    pub allow_list_proof: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> CloseAllowlistProofCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CloseAllowlistProofCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            global_config: accounts.global_config,
            gumball_guard: accounts.gumball_guard,
            allow_list_proof: accounts.allow_list_proof,
            system_program: accounts.system_program,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.global_config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.gumball_guard.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.allow_list_proof.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = CloseAllowlistProofInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::GUMBALL_GUARD_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.global_config.clone());
        account_infos.push(self.gumball_guard.clone());
        account_infos.push(self.allow_list_proof.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CloseAllowlistProof` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[]` global_config
///   2. `[]` gumball_guard
///   3. `[writable]` allow_list_proof
///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct CloseAllowlistProofCpiBuilder<'a, 'b> {
    instruction: Box<CloseAllowlistProofCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CloseAllowlistProofCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CloseAllowlistProofCpiBuilderInstruction {
            __program: program,
            authority: None,
            global_config: None,
            gumball_guard: None,
            allow_list_proof: None,
            system_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The protocol authority — must match global_config.account_fee_authority and receives rent.
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    /// The GlobalConfig PDA — used to validate the authority.
    #[inline(always)]
    pub fn global_config(
        &mut self,
        global_config: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.global_config = Some(global_config);
        self
    }
    /// The gumball guard that this proof was created under. Must be closed (empty).
    #[inline(always)]
    pub fn gumball_guard(
        &mut self,
        gumball_guard: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.gumball_guard = Some(gumball_guard);
        self
    }
    /// The AllowListProof PDA to close.
    #[inline(always)]
    pub fn allow_list_proof(
        &mut self,
        allow_list_proof: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.allow_list_proof = Some(allow_list_proof);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = CloseAllowlistProofCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            global_config: self
                .instruction
                .global_config
                .expect("global_config is not set"),

            gumball_guard: self
                .instruction
                .gumball_guard
                .expect("gumball_guard is not set"),

            allow_list_proof: self
                .instruction
                .allow_list_proof
                .expect("allow_list_proof is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CloseAllowlistProofCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    global_config: Option<&'b solana_account_info::AccountInfo<'a>>,
    gumball_guard: Option<&'b solana_account_info::AccountInfo<'a>>,
    allow_list_proof: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
