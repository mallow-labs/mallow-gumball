//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const INCREMENT_TOTAL_REVENUE_DISCRIMINATOR: [u8; 8] = [197, 168, 14, 157, 91, 203, 104, 102];

/// Accounts.
#[derive(Debug)]
pub struct IncrementTotalRevenue {
    /// Gumball machine account.
    pub gumball_machine: solana_address::Address,
    /// Gumball machine mint authority (mint only allowed for the mint_authority).
    pub mint_authority: solana_address::Address,
}

impl IncrementTotalRevenue {
    pub fn instruction(
        &self,
        args: IncrementTotalRevenueInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: IncrementTotalRevenueInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.gumball_machine,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.mint_authority,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = IncrementTotalRevenueInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::MALLOW_GUMBALL_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct IncrementTotalRevenueInstructionData {
    discriminator: [u8; 8],
}

impl IncrementTotalRevenueInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [197, 168, 14, 157, 91, 203, 104, 102],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for IncrementTotalRevenueInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct IncrementTotalRevenueInstructionArgs {
    pub revenue: u64,
}

impl IncrementTotalRevenueInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `IncrementTotalRevenue`.
///
/// ### Accounts:
///
///   0. `[writable]` gumball_machine
///   1. `[signer]` mint_authority
#[derive(Clone, Debug, Default)]
pub struct IncrementTotalRevenueBuilder {
    gumball_machine: Option<solana_address::Address>,
    mint_authority: Option<solana_address::Address>,
    revenue: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl IncrementTotalRevenueBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Gumball machine account.
    #[inline(always)]
    pub fn gumball_machine(&mut self, gumball_machine: solana_address::Address) -> &mut Self {
        self.gumball_machine = Some(gumball_machine);
        self
    }
    /// Gumball machine mint authority (mint only allowed for the mint_authority).
    #[inline(always)]
    pub fn mint_authority(&mut self, mint_authority: solana_address::Address) -> &mut Self {
        self.mint_authority = Some(mint_authority);
        self
    }
    #[inline(always)]
    pub fn revenue(&mut self, revenue: u64) -> &mut Self {
        self.revenue = Some(revenue);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = IncrementTotalRevenue {
            gumball_machine: self.gumball_machine.expect("gumball_machine is not set"),
            mint_authority: self.mint_authority.expect("mint_authority is not set"),
        };
        let args = IncrementTotalRevenueInstructionArgs {
            revenue: self.revenue.clone().expect("revenue is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `increment_total_revenue` CPI accounts.
pub struct IncrementTotalRevenueCpiAccounts<'a, 'b> {
    /// Gumball machine account.
    pub gumball_machine: &'b solana_account_info::AccountInfo<'a>,
    /// Gumball machine mint authority (mint only allowed for the mint_authority).
    pub mint_authority: &'b solana_account_info::AccountInfo<'a>,
}

/// `increment_total_revenue` CPI instruction.
pub struct IncrementTotalRevenueCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Gumball machine account.
    pub gumball_machine: &'b solana_account_info::AccountInfo<'a>,
    /// Gumball machine mint authority (mint only allowed for the mint_authority).
    pub mint_authority: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: IncrementTotalRevenueInstructionArgs,
}

impl<'a, 'b> IncrementTotalRevenueCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: IncrementTotalRevenueCpiAccounts<'a, 'b>,
        args: IncrementTotalRevenueInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            gumball_machine: accounts.gumball_machine,
            mint_authority: accounts.mint_authority,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.gumball_machine.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.mint_authority.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = IncrementTotalRevenueInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::MALLOW_GUMBALL_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.gumball_machine.clone());
        account_infos.push(self.mint_authority.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `IncrementTotalRevenue` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` gumball_machine
///   1. `[signer]` mint_authority
#[derive(Clone, Debug)]
pub struct IncrementTotalRevenueCpiBuilder<'a, 'b> {
    instruction: Box<IncrementTotalRevenueCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> IncrementTotalRevenueCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(IncrementTotalRevenueCpiBuilderInstruction {
            __program: program,
            gumball_machine: None,
            mint_authority: None,
            revenue: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Gumball machine account.
    #[inline(always)]
    pub fn gumball_machine(
        &mut self,
        gumball_machine: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.gumball_machine = Some(gumball_machine);
        self
    }
    /// Gumball machine mint authority (mint only allowed for the mint_authority).
    #[inline(always)]
    pub fn mint_authority(
        &mut self,
        mint_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.mint_authority = Some(mint_authority);
        self
    }
    #[inline(always)]
    pub fn revenue(&mut self, revenue: u64) -> &mut Self {
        self.instruction.revenue = Some(revenue);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = IncrementTotalRevenueInstructionArgs {
            revenue: self
                .instruction
                .revenue
                .clone()
                .expect("revenue is not set"),
        };
        let instruction = IncrementTotalRevenueCpi {
            __program: self.instruction.__program,

            gumball_machine: self
                .instruction
                .gumball_machine
                .expect("gumball_machine is not set"),

            mint_authority: self
                .instruction
                .mint_authority
                .expect("mint_authority is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct IncrementTotalRevenueCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    gumball_machine: Option<&'b solana_account_info::AccountInfo<'a>>,
    mint_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    revenue: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
