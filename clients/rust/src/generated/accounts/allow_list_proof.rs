//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

/// PDA to track whether an address has been validated or not.

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AllowListProof {
    pub timestamp: i64,
}

impl AllowListProof {
    pub const LEN: usize = 8;

    /// Prefix values used to generate a PDA for this account.
    ///
    /// Values are positional and appear in the following order:
    ///
    ///   0. `AllowListProof::PREFIX`
    ///   1. merkle_root (`[u8; 32]`)
    ///   2. user (`Address`)
    ///   3. gumball_guard (`Address`)
    ///   4. machine (`Address`)
    pub const PREFIX: &'static [u8] = "allow_list".as_bytes();

    pub fn create_pda(
        merkle_root: [u8; 32],
        user: Address,
        gumball_guard: Address,
        machine: Address,
        bump: u8,
    ) -> Result<solana_address::Address, solana_address::error::AddressError> {
        solana_address::Address::create_program_address(
            &[
                "allow_list".as_bytes(),
                &merkle_root,
                user.as_ref(),
                gumball_guard.as_ref(),
                machine.as_ref(),
                &[bump],
            ],
            &crate::GUMBALL_GUARD_ID,
        )
    }

    pub fn find_pda(
        merkle_root: [u8; 32],
        user: &Address,
        gumball_guard: &Address,
        machine: &Address,
    ) -> (solana_address::Address, u8) {
        solana_address::Address::find_program_address(
            &[
                "allow_list".as_bytes(),
                &merkle_root,
                user.as_ref(),
                gumball_guard.as_ref(),
                machine.as_ref(),
            ],
            &crate::GUMBALL_GUARD_ID,
        )
    }

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for AllowListProof {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_allow_list_proof(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<AllowListProof>, std::io::Error> {
    let accounts = fetch_all_allow_list_proof(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_allow_list_proof(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<AllowListProof>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<AllowListProof>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        let data = AllowListProof::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_allow_list_proof(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<AllowListProof>, std::io::Error> {
    let accounts = fetch_all_maybe_allow_list_proof(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_allow_list_proof(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<AllowListProof>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<AllowListProof>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = AllowListProof::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for AllowListProof {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for AllowListProof {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for AllowListProof {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::GUMBALL_GUARD_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for AllowListProof {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for AllowListProof {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
